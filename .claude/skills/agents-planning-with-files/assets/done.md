# Done — append-only log

<!--
Working memory, gitignored. APPEND ONLY — never rewrite or delete entries.
Each completed unit of work gets one entry: what, commit sha (if any), when.
Multi-actor shared tree: add under your own `## @<actor-id>` heading; never
touch anyone else's entries.
-->

## @<actor-id>

- [<HH:MM>] <what was finished> — <commit sha or "no commit">
- [<HH:MM>] <what was finished> — <commit sha or "no commit">
