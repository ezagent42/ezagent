# Operator + CustomerFeed Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** T1A.3 (operator takeover with Turn.claim→RuleStore.disable→edit→settle→enable) + T1A.4 (CustomerFeed :pull via Phoenix.PubSub.subscribe + {:customer_delivery})

---
### Task 1: operator_live.ex — Turn takeover

**File:** Modify `apps/ezagent_plugin_autoservice/lib/ezagent_plugin_autoservice/operator_live.ex`

Add handle_event "claim" that:
1. TurnAdapter.claim_turn(session_uri, turn_id, %{operator_uri: op_uri})
2. RuleStore.disable(rule_id) — find the routing rule for this session, set enabled=false
3. Subscribe to CustomerFeed.topic(session_uri) for real-time delivery

Add handle_event "settle" that:
1. TurnAdapter.settle_turn(session_uri, turn_id)
2. RuleStore.enable(rule_id) — restore agent routing
3. Post operator message to session via chat.send

Test: verify claim→disable→edit→settle→enable flow. Commit.

### Task 2: customer_live.ex — CustomerFeed subscription

**File:** Modify `apps/ezagent_plugin_autoservice/lib/ezagent_plugin_autoservice/customer_live.ex`

Replace session_events_topic subscription with:
```elixir
if connected?(socket) do
  Phoenix.PubSub.subscribe(EzagentCore.PubSub, Ezagent.Socialware.CustomerFeed.topic(session_uri))
end
```

Handle `{:customer_delivery, %{message_ids: ids}}` by calling `CustomerFeed.snapshot(session_uri, token)` to pull visible messages.

Add client-side message echo (PR #715 #10): when customer sends message, optimistically append to messages list. Merge with CustomerFeed deliveries by timestamp.

Add "AI客服" label for system-principal/agent senders (PR #715 #12).

Test: verify subscription, delivery handling, message echo. Commit.

### Task 3: Verify

Run `mix test apps/ezagent_plugin_autoservice/` → PASS. Commit.

**Plan complete.**
