# 员工工作台 — 页面 UI 设计文档

> 状态：Draft v0.1（2026-06-12）
> 配套：`2026-06-12-employee-dashboard-design.md`（架构与分期）；需求源 Module B
> 本文件回答两个问题：**页面怎么布局**、**每个区域是什么功能**

---

## 0. 页面清单与导航关系

```
/workbench                     页面 A · 员工落地页(总览 dashboard)     [PR-1]
/workbench?session=<uri>       页面 B · 会话工作台(三栏)              [PR-2/3]
/workbench/performance         页面 C · 我的业绩 + operator-agent      [PR-4]
/workbench/sandbox             页面 D · 沙箱陪练                       [PR-5]
```

- 全部挂在 AppShell 统一外壳下（复用 avatar / 通知；**员工 perspective 不出
  ⌘K 命令面板**——那是 admin/操作员的工具，员工界面保持极简），新增
  `perspective: :workbench`
- 单 LiveView + `handle_params` 路由（A↔B 同一 LV 切视图，复用 admin_live 的
  `?session=` 模式）；C / D 独立 LV
- 身份：`:require_entity`。admin 进入看到的内容一致（admin 是员工超集）

---

## 1. 页面 A · 员工落地页 `/workbench`

> 手册 B.1：登录后看到 ① 授权渠道列表 ② 负责的客户群体 ③ 业绩看板

```
┌──────────────────────────────────────────────────────────────────┐
│ AppShell: [logo] 工作台                         [🔔] [👤 张三 ▾] │
├──────────────────────────────────────────────────────────────────┤
│  早上好，张三 — 您有 2 个会话需要关注                    🔴 2     │
│                                                                  │
│  ┌─ A1 我的渠道 ──────────────┐  ┌─ A2 我的业绩(今日) ─────────┐ │
│  │ 🟢 飞书·售前群     3 会话   │  │  会话 12   接管 3           │ │
│  │ 🔴 Web widget      1 待回应 │  │  平均首响 28s   CSAT —      │ │
│  │ 🟡 WhatsApp        2 会话   │  │  [详情 →]  [问 operator-agent]│ │
│  │ （admin 授权的渠道才显示）   │  └────────────────────────────┘ │
│  └────────────────────────────┘  ┌─ A4 沙箱陪练 ───────────────┐ │
│  ┌─ A3 活跃会话 ──────────────────│  上手新场景前先练一轮        │ │
│  │ 🔴 Marie(WhatsApp) AI接待中 "Is it waterproof?"  2m前 [进入] │ │
│  │ 🟢 李老板(飞书)   您已接管   "发票开好了吗"      10s前 [进入] │ │
│  │ 🟡 visitor_8f2(Web) AI接待中 "do you ship to EU" 40s前 [进入] │ │
│  └──────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

### 区域功能

| 区 | 功能 | 数据源 | 交互 |
|---|---|---|---|
| 顶部问候条 | 姓名 + 需关注数（红色 SLA 会话计数），点击数字 → 页面 B 并筛"需要关注" | SLA 聚合 | 点击跳转 |
| **A1 我的渠道** | admin 授权给我的渠道列表；每行：渠道 icon+名、**聚合 SLA 点**（该渠道最差会话的颜色）、活跃会话数 | workspace member declaration 的 `channels:` 标注 | 点渠道 → 页面 B 左栏定位到该渠道分组 |
| **A2 我的业绩** | 今日 4 指标：会话数 / 接管次数 / 平均首响 / CSAT（占位"—"，标 tooltip"评分入口未开通"） | 业绩聚合查询（PR-4 前显示占位骨架） | [详情] → 页面 C；[问 operator-agent] → 页面 C 并聚焦聊天框 |
| **A3 活跃会话** | 跨渠道合并的活跃会话表，每行：SLA 点、客户标识、渠道、**当前处理者**（"AI 接待中" / "您已接管" / "王五处理中"）、最后消息摘要、距今时间 | `SessionContext.list_sessions_for` + 授权过滤 + 末条消息 | [进入] → 页面 B 打开该会话 |
| **A4 沙箱入口** | 一句话引导 + 进入按钮 | — | → 页面 D |

**空态**：无授权渠道 → A1/A3 显示"管理员还没有给您分配渠道，请联系 admin"；
无活跃会话 → A3 显示"暂无会话，客户来了会第一时间出现在这里"。

---

## 2. 页面 B · 会话工作台 `/workbench?session=<uri>`

> 手册 B.2 三栏 + B.3 三模式。**这是核心页面。**

```
┌──────────────────────────────────────────────────────────────────────────┐
│ AppShell                                                                  │
├──────────┬─────────────────────────────────────────────┬─────────────────┤
│ B1 导航栏 │ B2 会话区                                    │ B3 AI 协作面板  │
│          │ ┌─────────────────────────────────────────┐ │                 │
│ 筛选:     │ │ Marie · WhatsApp · EN→中  [Auto|Copilot|Takeover]│ │ 本会话 AI 同事  │
│ [全部]    │ ├─────────────────────────────────────────┤ │ ● 接待AI  空闲  │
│ [🔴需关注]│ │ 客户: Is it waterproof?            10:02 │ │ ● 业务AI  思考中│
│ [我接管的]│ │ 🤖接待AI: Yes! The ZL series is...  10:02 │ │ ● 翻译AI  空闲  │
│          │ │ 客户: reviews from US buyers?      10:04 │ │                 │
│ ▾ WhatsApp│ │ ┄┄🔒您→业务AI: 提一下 RE100 认证┄┄ 10:05 │ │ 客户上下文      │
│  🔴 Marie │ │ 🤖业务AI: We're RE100 certified...  10:05 │ │ 来源: WhatsApp  │
│  🟢 Tom   │ │ 👤您: 我帮您申请一个样品折扣       10:07 │ │ 历史会话: 3     │
│ ▾ 飞书    │ ├─────────────────────────────────────────┤ │ 上次意图: 比价  │
│  🟢 李老板 │ │ B2c composer(随模式变化,见下表)          │ │                 │
│ ▾ Web     │ │ [🔒 私密建议,仅 AI 可见…        ] [发送] │ │ [+ 邀请AI同事]  │
│  🟡 visitor│ └─────────────────────────────────────────┘ │                 │
└──────────┴─────────────────────────────────────────────┴─────────────────┘
```

### B1 · 导航栏（左，可折叠）

| 功能 | 说明 |
|---|---|
| 筛选 chips | 全部 / 🔴需关注（SLA 红）/ 我接管的 |
| 渠道分组列表 | 按渠道分组的活跃会话；每行：**SLA 点 + 客户标识 + 未读数** |
| SLA 点语义（v0） | 最后一条**客户**消息未被回应的时长：🟢 <30s ｜ 🟡 30s–2min ｜ 🔴 >2min；turn 处于 `:awaiting_human`（claim 后未 settle）**恒黄**——扣着 visibility 等员工就是"需要关注"。阈值进 config，5s tick 刷新 |
| 实时性 | PubSub 订阅 session 事件（复用 admin_live 订阅模式），新消息即时插入 + SLA 重算 |

### B2 · 会话区（中）

**B2a header**：客户标识 · 渠道 · 语言对（翻译 AI 检测）· **三模式切换器**（页面最重要的控件）

| 模式 | 切换器状态 | 含义（手册 B.3） | Turn 语义（决策 2026-06-12：建在 socialware Turn 上） |
|---|---|---|---|
| **Auto** | 默认 | AI 独立处理，员工纯旁观 | turn 不 claim，`open→…→settle` 自动走完 |
| **Copilot** | 一键切 | 员工私密建议给 AI，客户不可见 | `turn.claim(by: 我)` → `:awaiting_human`，**visibility 扣住**；建议后 AI 重 compose，员工 settle 放行 |
| **Takeover** | 一键切 | 员工直接发消息，客户感觉是真人 | claim 后员工自写 turn result → settle |

**模式指示器不是独立 UI 状态——它是当前 turn 的 `mode/owner/status` 投影**。
切换规则：任何时候可一键切换；**每次切换在消息流插入一条系统提示**（"您于 10:05
进入 Copilot 模式"）——审计 + 团队可追溯；Copilot/Takeover → Auto 切回时弹确认，
二选一：**放行**（settle，客户看到当前结果）或**丢弃**（cancel）。
`turn.owner` 是别人时切换器整体禁用（见 §5 会话占用）。

**B2b 消息流**（复用 ConversationView + 新消息类型样式）：

| 消息类型 | 样式 |
|---|---|
| 客户消息 | 左侧气泡 |
| AI 消息 | 左侧气泡 + 🤖 + AI 同事名 |
| 员工 Takeover 消息 | 右侧实心气泡 + 👤 |
| **Copilot 私密建议** | 右侧**虚线边框 + 🔒 + "仅 AI 可见"角标**——视觉上必须与公开消息截然不同（防员工误判客户可见性，这是本页面最大的 UX 风险点） |
| 模式切换系统提示 | 居中灰色细字 |

**B2c composer**（随模式变形——同一个输入框三种皮）：

| 模式 | composer 状态 |
|---|---|
| Auto | **禁用**，占位文案："观察中——切到 Copilot 给 AI 提建议，或 Takeover 直接回复" |
| Copilot | 可输入；输入框带 🔒 前缀样式；placeholder "私密建议，仅 AI 可见"；发送按钮文案"发给 AI" |
| Takeover | 可输入；placeholder "以您的身份直接回复客户"；发送按钮"发送" |

### B3 · AI 协作面板（右）

| 功能 | 说明 |
|---|---|
| 本会话 AI 成员 | 列表：AI 同事名 + 状态点（空闲/思考中——接 `loom:gen_progress` 类事件 v0 简化为"最近 10s 有无产出"） |
| 客户上下文卡 | 来源渠道 / 历史会话数 / 上次意图（v0：上一会话末条消息摘要）——支撑手册 C.3"跨渠道认识您" |
| [+ 邀请 AI 同事] | 从 AI 同事库选一个加入本会话（复用既有 invite 流） |
| （后置）转交员工 | 转给其他坐席，PR 范围外 |

---

## 3. 页面 C · 我的业绩 `/workbench/performance`

> 手册 B.4：业绩看板 + **和 operator-agent 对话**。

```
┌──────────────────────────────────────────────────────────────────┐
│ ┌─ KPI 行 ─────────────────────────────────────────────────────┐ │
│ │ 今日会话 12 │ 接管 3 │ 平均首响 28s │ 平均时长 8m │ CSAT — │ │
│ └──────────────────────────────────────────────────────────────┘ │
│ ┌─ C2 近7日会话趋势(bar) ──────┐ ┌─ C3 客户类型分布 ───────────┐ │
│ │ ▂▄▆▃▇▅▆                      │ │ 渠道×语言矩阵/简单饼图       │ │
│ └──────────────────────────────┘ └─────────────────────────────┘ │
│ ┌─ C4 operator-agent ──────────────────────────────────────────┐ │
│ │ 🤖: 您好,想了解今天的业绩吗?                                  │ │
│ │ [我今天接了几个会话?] [哪类客户最难?] [帮我在沙箱跑一下]      │ │
│ │ [输入问题…                                          ] [发送] │ │
│ └──────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

| 区 | 功能 |
|---|---|
| C1 KPI 行 | 5 指标卡；CSAT 灰显"—"+ tooltip；可切 今日/本周/本月 |
| C2 趋势 | 近 7 日会话量 bar（纯 HEEx/SVG，不引图表库） |
| C3 分布 | 客户类型 = 渠道 × 检测语言 的粗分布 |
| C4 operator-agent | 对话区 + **预置问题 chips**（对照手册 B.4 对话示例）；"帮我在沙箱跑一下" chip 在 PR-5 前回复"沙箱即将上线" |

---

## 4. 页面 D · 沙箱陪练 `/workbench/sandbox`

> 手册 B.5：模拟客户 + 导师 AI 点评 + 复盘。

**D1 场景选择** → **D2 练习中** → **D3 复盘** 三步流：

```
D2 练习中:
┌────────────────────────────┬──────────────────────────────┐
│ 模拟客户对话                │ 您的工作台(复用页面 B 中栏)    │
│ 🎭 customer-simulator:     │ [Auto|Copilot|Takeover]      │
│ "你们这个太贵了,降点价"      │  …同真实工作台操作…           │
├────────────────────────────┴──────────────────────────────┤
│ 🎓 导师AI: 议价场景先共情再报方案——刚才直接报价了,建议补一句… │
└────────────────────────────────────────────────────────────┘
```

| 区 | 功能 |
|---|---|
| D1 场景卡 | 3 个内置：日语议价 / 投诉处理 / 大促咨询；显示"已练 N 轮" |
| D2 练习区 | customer-simulator AI 扮演客户（场景 persona prompt）；员工用**与页面 B 完全相同的组件**操作（练的就是真实界面）；底部导师 AI 实时点评条 |
| D3 复盘 | 本轮统计（响应速度/模式切换次数）+ 导师总结 + [再练一轮] |

---

## 5. 横切设计

| 项 | 设计 |
|---|---|
| **可见性安全** | Copilot 私密消息是本设计最大风险面：样式上锁形角标 + 虚线必须无歧义；实现上对照 socialware 红线——`:operator_only` 类内容**不经任何路径**（渠道镜像 / 分享 / SSE）到达客户 |
| 会话占用 | `turn.owner` 是其他员工（已 claim）的会话：模式切换器禁用 + 显示"王五处理中"，可观察不可操作（Turn 单 owner 模型天然保证） |
| 降级 | AI 成员全部无响应 >30s：B3 面板红色提示"AI 同事未响应，建议 Takeover" |
| 响应式 | v0 桌面优先；窄屏 B1/B3 折叠为抽屉 |
| i18n | 全部文案走 gettext（员工界面中文优先） |
| 视觉 | 沿用 EzagentDomainUi 的 Tailwind token / Components / Primitives，不引新 UI 库 |

## 6. 功能 ↔ PR 对照

| 页面/区域 | PR |
|---|---|
| A 全部（A2 占位骨架） | PR-1 |
| B1 + B2（observe-only）+ B3 + SLA | PR-2 |
| B2a 三模式切换 + B2c 变形 composer + 🔒 私密消息 | PR-3 |
| C 全部 + A2 接真数据 | PR-4 |
| D 全部 | PR-5 |
