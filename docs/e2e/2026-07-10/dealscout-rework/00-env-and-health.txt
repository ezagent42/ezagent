# 00 环境与健康检查（dealscout rework 段5 e2e，2026-07-10）

- 分支：feat/sw-dealscout-rework（工作树 .claude/worktrees/sw-dealscout-rework，tip = 段4 三 commit 之上）
- 工具链：mise exec elixir@1.18.4-otp-27 erlang@27.3.4.13 -- mix ...
- DB：docker PG ezagent-pg-compat-audit-postgres（127.0.0.1:55432，库 ezagent_pg_compat_dev）
  - 开测前 `mix ecto.reset`（drop+create+migrate+seed）清掉旧实验污染
  - `mix run scripts/world_e2e_seed.exs` → admin@ezagent.chat / $ADMIN_PW（见 docs/guide/world-e2e-seed.md）
- server：`elixir --name ezagent_runtime@127.0.0.1 -S mix phx.server`（后台，beam PID 94707，端口 10042）
  - 健康：curl -fsS http://localhost:10042/_health → HTTP 200
  - 分布式 cookie 注记：boot 时 `Ezagent.Runtime.ensure_cookie!` 会用
    `$EZAGENT_HOME/default/runtime/cookie` 覆盖命令行 `--cookie`（runtime.ex:112
    `Node.set_cookie`）—— erpc/dispatch 辅助脚本一律读该文件（内容不入库）。
- 浏览器：agent-browser（headless Chromium），一律 http://world.localhost:10042（HSTS 坑不用 world.ezagent.chat）
- 环境搭建性修复（不涉库代码）：apps/ezagent_web/assets 与 apps/ezagent_plugin_world/assets
  缺 node_modules → pnpm install（React 岛挂不上的已知环境坑，同 kanban rework 段4 e2e 注记）
