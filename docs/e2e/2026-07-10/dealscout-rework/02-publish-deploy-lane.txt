# 02 发布证据 — 部署级 seed 车道（dealscout rework 段5，2026-07-10）

## 车道（照 kanban 先例）：server 节点内 seed! 拷贝 + scan_dir! 晚扫描发布

辅助脚本（/tmp/dealscout_publish_e2e.exs，erpc 进运行中的 server 节点
ezagent_runtime@127.0.0.1）依次驱动：

1. `Ezagent.Home.SocialwareSeed.seed!()` —— priv/socialware_seed 的 dealscout
   包幂等拷贝进部署目录 `$EZAGENT_HOME/default/socialware/dealscout/`
   （拷贝后 `diff` 部署侧与本分支 `apps/ezagent_web/priv/socialware_seed/
   dealscout/manifest.yaml` → byte-identical）；
2. `Ezagent.Socialware.ManifestSeed.scan_dir!("/tmp/dealscout-deploy-only",
   source: "deploy")` —— 只含 dealscout 包的目录（symlink 指向部署目录真实包，
   避开 autoservice 按字母序先扫 fail-loud 的既有无关问题，同 kanban 注记）。

## 脚本实际输出（首跑）

```
03:31:29.211 [info] socialware manifest seed: dealscout (deploy) → published
scan_dir! (in server node): [
  %{
    name: "dealscout",
    path: "/tmp/dealscout-deploy-only/dealscout/manifest.yaml",
    result: :published
  }
]
```

## 注记

- dev 环境 boot scan 关闭（config/config.exs socialware_manifest_boot_scan
  prod-only），故显式在 server 节点驱动同一晚扫描车道（语义等价：全插件
  boot 完成后扫部署目录）。
- 发布走完整 governed import lane：parse → ManifestResolver.resolve →
  Conformance.check_candidate（15/15）→ publish_or_upgrade。

## DB 交叉印证（ezagent_pg_compat_dev @ 127.0.0.1:55432）

```
SELECT subject_uri, layer, key FROM socialware_config_pointers WHERE subject_uri = 'socialware:dealscout';
socialware:dealscout | workspace | socialware
```
