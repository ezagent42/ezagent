# 03e 安装步骤 DB 交叉印证（kanban rework 收口 e2e，2026-07-10）

## create_session：#1294 后现实——同步秒建成，无红条

真向导（应用=Kanban 看板团队，两 role 槽 cc-headless×Fresh，名称 kanban-final-0710）
点「创建」→ UI 直接切入新会话（03b 截图，无任何红条），header 秒到「3 成员 · 0 轮次」。

invocations 表（authz granted，无 exception）：

  target      workspace://system?action=workspace.create_session
  at          2026-07-10 03:33:37.490515Z
  duration_us 1910575    （≈1.91s，远低于 5s 预算，不触超时）

## 成员即时在线（03c 截图）

li[data-kind=agent][data-online=true] ×2（kanban-assistant / dev-together）+ Admin(user, online)。
已装 Socialware = 1（Kanban 看板团队 — kanban-assistant, dev-together）。

## relay-back 规则装入（materialize 时从 manifest 装入，routing_rules id=2）

  matcher_data | {"type": "and", "items": [{"arg": "__done__", "type": "text_contains"},
                                           {"arg": "dev-together", "type": "from_role"}]}
  receivers    | {$role:a2FuYmFuLWFzc2lzdGFudA}   -- base64url("kanban-assistant")，零实例 URI
  rule_set     | relay-back
  position     | 0
  enabled      | t
  created_by   | session://system/socialware-install-kanban/kanban-final-0710

与 manifest.yaml routing_rules 逐字一致。

## UI 注记（03d 截图）

右侧面板「高级规则 0」是预期显示：该列表只呈现 matcher 直接锚定本会话的规则
（conversation_data.ex list_session_routing_rules → matcher_targets_session?），
relay-back 属 rule_set 规则不在其列——装入证据以上方 DB 行为准。
