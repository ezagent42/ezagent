# 08b cc 真脑现状实测（2026-07-10 收口 e2e）— 被 #1309 挡住

真键盘 autocomplete @kanban-assistant「请把看板现状汇报一下（真脑现状实测）」：

- 04:00:47Z admin 消息入库（f0d17ad1147d09b2，结构化 mention）
- 04:00:48Z kanban-assistant（entity://system/agent/24b6e087-…）回包：
  **`Not logged in · Please run /login`**（sidecar 已热，~2s；截图 08a）
- 同 transcript 前段 07 步：dev-together 首包 23s 也是同样 401（07c 截图）

## 分层判读（口径与 README 一致）

- 送达层 ✓：mention 门控路由 + 进程被拉起 + 秒级回包——**回 401 本身证明管道全通，
  死的只是登录一环**。
- 大脑层 ✗：assistant 自主推卡 / dev-together 自主发 __done__ 未曾端到端发生过。

## 根因 = issue #1309（与本 PR 无关，修复 = cc 插件一行）

cc-headless 的 template class `CcHeadlessAgent` 漏 delegate `host_login_dir/0`
（cc_headless_agent.ex；`CcAgent` 有），`CredentialAdapter.host_login_source_dir/1`
的 function_exported? gate 判 `:none` → #1209 宿主登录收养链对 cc-headless 静默
no-op → 物化 config_dir 里根本没有 .credentials.json（无副本，非过期）。

本次全新库复核（铁证）：
- 宿主 ~/.claude/.credentials.json 有效：expiresAt=1783674628214，测时余量 ~5.2h
  （**非宿主凭证过期**，只记 expiresAt 不抄 token）
- 两个 agent 的物化 config_dir（~/.ezagent/default/cc-headless-agents/system/<id>/）
  均 **无 .credentials.json**
- 三张 credential 表全空：user_default_credential_sources=0 /
  workspace_shared_credential_sources=0 / credential_grants=0

首发根因分析（file:line + erpc 正反证）见 07-09 调查（已并入 #1309），
kanban manifest 两 role 恰全是 cc-headless，落在 #1209 集成测试只测 flavor "cc" 的覆盖缺口里。
