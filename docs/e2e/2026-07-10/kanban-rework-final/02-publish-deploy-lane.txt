# 02 发布证据 — 部署级 seed 车道（kanban rework 收口 e2e，2026-07-10）

## deploy-seed copy（mix run 脚本调 SocialwareSeed.seed!，旧部署包已先移开备份保证拷的是本分支版本）

[info] socialware deploy-seed: kanban → /home/yaosh/.ezagent/default/socialware/kanban
deploy dir: /home/yaosh/.ezagent/default/socialware
packages: ["autoservice", "dealscout", "hello", "kanban"]

拷贝后核对：部署目录 manifest.yaml 与本分支
apps/ezagent_web/priv/socialware_seed/kanban/manifest.yaml **逐字节一致**（diff -q → SAME）。

## 晚扫描发布（erpc 进运行中的 server 节点 ezagent_runtime@127.0.0.1 跑 ManifestSeed.scan_dir!）

scan_dir!("/tmp/kanban-deploy-only") →
[%{name: "kanban", path: "/tmp/kanban-deploy-only/kanban/manifest.yaml", result: :published}]

（/tmp/kanban-deploy-only/kanban 是指向部署目录真实包
 $EZAGENT_HOME/default/socialware/kanban 的 symlink——整目录 scan 会在
 autoservice 处 fail-loud（本库无 recipe:autoservice-agent，按字母序先于 kanban），
 故用只含 kanban 的目录单独扫，被扫的是部署目录真实文件。
 注：Logger.info "kanban (deploy) → published" 经 erpc 的 group-leader 回传到
 probe 节点侧，server log 不落此行；以 scan_dir! 返回值 + 下方 DB 指针为准。）

## DB 交叉印证（ezagent_pg_compat_dev @ 127.0.0.1:55432）

SELECT subject_uri, layer, key FROM socialware_config_pointers WHERE subject_uri = 'socialware:kanban';
socialware:kanban|workspace|socialware

## 注记

- dev 环境 boot scan 关闭（config/config.exs socialware_manifest_boot_scan: prod-only，设计如此），
  故显式在 server 节点驱动同一晚扫描车道（语义等价：全插件 boot 完成后扫部署目录）。
- 插件 boot 零自发布（Decision #156）：发布完全由 manifest YAML 经 seed 车道驱动。
