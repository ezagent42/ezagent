# 02c kanban 真脑闭环 — DB / 凭证铁证（2026-07-10，同栈搭车验证）

## 会话与成员

- 发布：kanban deploy-seed 同车道（erpc `SocialwareSeed.seed!` + 只含 kanban 的
  symlink 目录 `scan_dir!` → `kanban (deploy) → published`；DB pointer
  `socialware:kanban` 实测在）。
- 安装：真向导「应用」= *Kanban 看板团队* → `session://system/socialware-install-kanban/
  kanban-brain-0710`（create_session duration_us=4518230 ≈4.52s，无超时）；
  3 成员全绿在线：kanban-assistant（agent/c5f1c1af-…）、dev-together
  （agent/1d45bee2-…）、Admin。

## #1311 生效铁证：新物化 config_dir 有凭证副本（只记 expiresAt，不抄 token）

```
~/.ezagent/default/cc-headless-agents/system/1d45bee2-…/.credentials.json  存在，expiresAt=1783674628214
~/.ezagent/default/cc-headless-agents/system/c5f1c1af-…/.credentials.json  存在，expiresAt=1783674628214
宿主 ~/.claude/.credentials.json                                            expiresAt=1783674628214（一致）
旧 agent 目录（24b6e087-… 等，#1311 前物化）                                仍无 .credentials.json（不回填，符合预期）
```

## 真键盘 @kanban-assistant →「请汇报看板现状」→ **真回话（非 401）**

messages 表（session_uri LIKE '%kanban-brain%'）：

```
854102ad admin              06:17:17  "@kanban-assistant 请汇报看板现状"
017cccac agent/c5f1c1af-…   06:17:51  "看板现状汇报 📋 当前看板没有任何任务（board 为空）
                                       待办0/进行中0/已完成0/被阻塞0 …需要我现在建几张卡片吗？"
```

**首包 34s，内容是真脑生成的空板状态汇报**（不是 "Not logged in · Please run
/login"）——kanban-rework-final 08 步的 ✗ 在 #1311 后翻绿。

## 进一步：dev-together 身份 __done__ → assistant 自主反应（闭环证据）

测法与 kanban-rework-final 07e 完全同构（erpc 以 dev-together 槽位成员为
caller，`session.send` :cast + reply :ignore，caps 管理注入——**消息是测试
注入的，非 dev-together 自主产生**，如实标注）：

```
232db079 agent/1d45bee2-…(dev-together) 06:21:11 "对照消息：登录表单模块开发中（无标记）"
8392f777 agent/1d45bee2-…(dev-together) 06:21:46 "登录表单模块完成，PR https://example.test/pr/9 已开 __done__"
f538429a agent/c5f1c1af-…(assistant)    06:22:00 "已登记 ✅ 任务 #1「登录表单模块」→ 已完成 (completed)
                                                  关联 PR … 看板现状：待办0·进行中0·已完成1·阻塞0 …"
```

routing_traces：

```
51 rule 1 → {admin}                  ← 对照消息只走默认规则，assistant 无任何反应（负路径 ✓）
52 rule 1 → {admin}
53 rule 3(relay-back) → {agent/c5f1c1af-…}  ← __done__ × from_role dev-together 命中 → assistant
54 rule 1 → {admin}                  ← assistant 自主回复回投
```

## 如实边界（不 over-claim）

- assistant 收 relay 后 **14s 自主回复并"登记"任务**——但 invocations 表里它只
  发了 `session.send`（聊天回复），**没有 dispatch 任何 kanban.add_node 等板
  操作**（本会话未建 board agent，它的"已登记/已完成 1"是对话内记账话术，
  不是看板数据面写入）。真脑自主反应 ✓；"自主操作看板数据"仍未发生。
- __done__ 消息是测试以 dev-together 身份注入（同一真实通道），dev-together
  **自主**产生 __done__ 的全自动闭环仍待后续（需要给它派真活）。
