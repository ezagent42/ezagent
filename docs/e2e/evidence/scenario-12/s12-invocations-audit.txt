scenario-12 dispatch 审计 — 2026-06-25, branch feat/product-gaps-f9-f12 (= main 8b673310)
查询:mix run --no-start scratch_audit.exs(只启 Repo,查 PG invocations + pg_tables)

== 1. DLQ/dead-letter/telemetry/dispatch/audit 表 ==
(无匹配表名 — public schema 里不存在任何此类持久化表)

== 2. zyli-test-1 相关 invocations(节选,全 authz=granted) ==
# scenario-08 单播(@zyli-echo-1 09:21:21):
09:21:21 send    granted s:zyli-test-1?action=session.send  <- e:user/admin
09:21:21 receive granted e:agent/zyli-echo-1?action=agent.receive <- session   # 仅 echo 收到
09:21:21 send    granted s:zyli-test-1?action=session.send  <- e:agent/zyli-echo-1  # echo 回
  → 同时刻无 zyli-curl-1 的 receive  ⇒ 单播成立

# scenario-08 单播(@zyli-curl-1 09:21:35):
09:21:35 send    granted s:zyli-test-1?action=session.send  <- e:user/admin
09:21:35 receive granted e:agent/zyli-curl-1?action=agent.receive <- session   # 仅 curl 收到
09:21:36 sync_result granted e:agent/zyli-curl-1?action=sync_result.sync_result
09:21:36 send    granted s:zyli-test-1?action=session.send  <- e:agent/zyli-curl-1  # curl 回
  → 同时刻无 zyli-echo-1 的 receive  ⇒ 单播成立

# scenario-09 非成员 @(echo_default 09:26:27 / e2e-test 09:27:12):
09:26:27 send granted s:zyli-test-1?action=session.send <- e:user/admin   (×2 = @echo_default)
09:27:12 send granted s:zyli-test-1?action=session.send <- e:user/admin   (×2 = @e2e-test)
  → 两者之后均无任何 agent.receive  ⇒ 非成员零投递成立
  → 但也无任何 DLQ/reject/unroutable 记录(见 §3)

# curl 往返(09:15:06→09:15:09)同样完整:send(user)→receive(curl)→sync_result→send(curl 回)

== 3. unroutable/dlq/reject/no_match/zero_match invocations ==
(全表近期:无任何匹配 — 零结果)

== 日志侧(phx_proxy.log)==
grep zero.?match|unroutable|dlq|no_recipient|reject  → 零命中(无 telemetry 日志)

== 结论 ==
✅ 每个 agent 往返有 receive+send invocation,全 granted,可追溯
✅ mention 单播在 dispatch 层成立(只有被 @ 的 agent 有 agent.receive)
✅ 非成员零投递在 dispatch 层成立(非成员无 agent.receive)
📌 零匹配(无路由消息 / @非成员)→ 无 DLQ表 / 无 reject invocation / 无 telemetry 日志
   ⇒ "没人接收时,目前没人知道"。是否违反 P22 DLQ-on-zero-match 需 Allen 判定
     (可能 chat 零匹配本就 no-op 设计 / 可能 DLQ-on-zero-match 缺口)。
