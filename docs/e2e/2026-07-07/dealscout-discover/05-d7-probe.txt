D⑦ 探针（2026-07-07 02:06 本地，dealscout 组合会话 dealscout-e2e-r2）

动作：owner(admin) 在 world console 会话 composer 发不带 @mention 的消息：
  "这些线索里有哪些适合早期基金关注?请帮我整理一下"
（公开面 /socialware/chat 输入框对已登录 owner 仍禁用"登录后参与"，点登录回跳后依旧
 禁用 —— owner 无法从公开面发言，见 05a 截图；探针改在 owner console 面发送。）

DB 证据（messages 表）：
  id=4df592af0fdda335  mentions={}  routed_at 非空（已路由）

invocations 审计（18:06:05 之后全部 4 条）：
  session.send granted ×2（send 本体 + 审计重复行）
  snapshot.written ×2
  —— 零 agent.receive / user.receive；等待 40s+ 无任何 agent 响应，chat 无回帖。

结论：**不通（对 dealscout 组合会话）**。
  该消息经 Resolver 只命中 system_default 规则（$session_users/$mentions），
  sender 排除后无收件人；dealscout Definition 只声明 __dealscout_update__ 内容
  规则（刻意不带 hello 的 always→chat，kanban 红线），而 hello 的 orch_<name>
  orchestrator 是 hello 自己 app 会话的命令式 ensure，不给 dealscout 组合会话
  补 —— 所以 owner 的普通聊天在 dealscout 会话里没有任何 agent 响应者。
  #1208 是否修好 hello 自己会话的裸名 mention 与此正交，本探针未涉及裸名
  mention（发的是零 mention 消息）。
