CANARY AGENT-CALLABILITY — DOWNSTREAM READINESS
Date: 2026-07-13

Decision:
  READY WITH A KNOWN OPERATOR-UI LIMITATION for the next kanban-to-agent chain
  test. The complete hello-to-PR demo is not claimed here.

Callable target:
  Session: session://ezagent/socialware-install-orchestrator/acc-20260713t065612z-79e83afd
  Agent: entity://ezagent/agent/5825561c-cbd6-433e-a1ea-b45d681371b6
  Role/recipe: orchestrator
  Flavor: cc-deepseek
  Product call form: send `@orchestrator <task>` in the authenticated session
  chat through the sanctioned chat.send path.

Proof:
  - Unique nonce P1 received an exact ACK from this agent.
  - Different nonce P2 received an exact ACK from this agent.
  - Minimal development task T1 received ACCEPTED, Scope, a three-step Plan,
    and Status from the same agent and path.
  - Transcript message IDs/timestamps correlate with the agent bridge inbound
    deliveries and response tool calls.

Known limitation:
  The Terminal tab crashes its LiveView while JSON-encoding exec_pid. This does
  not break chat dispatch or bridge operation, but downstream operators cannot
  use the product Terminal pane to inspect the agent until the local fix is
  reviewed, deployed, and reverified.

Not authorized/performed:
  - no extra agent or dev-together member was created;
  - no real file edit, commit, push, or PR task was dispatched;
  - no test session cleanup was performed;
  - no deployment/configuration change followed the product test.

Secrets:
  None are required for this handoff and none are included.
