# 02 发布证据 — 部署级 seed 车道（kanban rework 段4，2026-07-09）

## deploy-seed copy（mix run /tmp/kanban_publish_e2e.exs，seed! 幂等拷贝）
[info] socialware deploy-seed: kanban → /home/yaosh/.ezagent/default/socialware/kanban
packages: ["hello", "kanban", "autoservice"]

## 晚扫描发布（erpc 进运行中的 server 节点 ezagent_runtime@127.0.0.1 跑 ManifestSeed.scan_dir!）
23:07:58.540 [info] socialware manifest seed: kanban (deploy) → published
scan_dir! (in server node): {"/home/yaosh/.ezagent/default/socialware", [{"kanban", :published, "/tmp/kanban-deploy-only/kanban/manifest.yaml"}]}

## 注记
- dev 环境 boot scan 关闭（config/config.exs:33 socialware_manifest_boot_scan: config_env() in [:prod]），故显式在 server 节点驱动同一晚扫描车道（语义等价：全插件 boot 完成后扫部署目录）。
- 被扫的 manifest 是部署目录 $EZAGENT_HOME/default/socialware/kanban/manifest.yaml（deploy-seed 从 apps/ezagent_web/priv/socialware_seed/kanban/ 拷贝，本分支版本；旧残留包已先移开备份）。
- 整目录 scan_dir! 在 autoservice 处 fail-loud（{:unknown_agent_recipe, "autoservice-agent"}，该 recipe 非本分支/本流程职责），按字母序先于 kanban，故用只含 kanban 包的目录（symlink 指向部署目录真实包）单独扫。

## DB 交叉印证（ezagent_pg_compat_dev @ 127.0.0.1:55432）

```
SELECT subject_uri, layer, key FROM socialware_config_pointers WHERE subject_uri = 'socialware:kanban';
socialware:kanban|workspace|socialware
```
