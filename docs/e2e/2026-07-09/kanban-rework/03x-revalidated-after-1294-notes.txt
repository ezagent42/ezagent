# 03x 重验记录 — #1294 合入后 create_session 复测（2026-07-10）

## 背景
原 03 步标注"UI 有已知 5s create_session 超时红条（#1279），后台建成，如实标注"。
main b11c81de3 含 gaga 的 #1294 修复（创建事务解耦 orchestrator eager-spawn +
cc PTY 等 config_dir 物化后再起）。本分支 rebase 过该 base 后重验此步骤。

## 环境
- 分支 feat/sw-kanban-rework（rebase 含 #1294，b11c81de3 为 ancestor）
- mix ecto.reset + seeds + scripts/world_e2e_seed.exs（全新库）
- kanban 经部署级 seed 车道重新发布（SocialwareSeed.seed! + erpc 进 server 节点
  ManifestSeed.scan_dir!("/tmp/kanban-deploy-only") → kanban (deploy) → published）
- server：mix phx.server 后台，beam PID 56979，端口 10042，_health 200

## 复测过程（真浏览器 agent-browser，http://world.localhost:10042）
1. admin 登录 → 新建会话向导：名称 kanban-1294-reverify、应用选「Kanban 看板团队」，
   向导展示两个 role 槽（kanban-assistant / dev-together × cc-headless，Fresh）
   → 03x-revalidated-after-1294-wizard.png
2. 点「创建」→ **无红条，UI 直接切入新会话**（对比旧 03b 的超时红条）
   → 03x-revalidated-after-1294-created-no-redbar.png（header "3 成员 · 0 轮次"）
3. 成员面板：kanban-assistant / dev-together 绿点在线 + Admin，已装 Socialware=1
   （Kanban 看板团队）→ 03x-revalidated-after-1294-members.png

## DB 交叉印证（ezagent_pg_compat_dev @ 127.0.0.1:55432）

create_session invocation（invocations 表，authz=granted、无 exception）：

  target   workspace://system?action=workspace.create_session
  at       2026-07-10 02:32:06.195189Z
  duration_us = 1137096   （≈1.14s，远低于 5s 预算 → 不再超时）

relay-back 规则照常装入（routing_rules id=2，与 manifest YAML 逐字一致）：

  {"type":"and","items":[{"arg":"__done__","type":"text_contains"},
                         {"arg":"dev-together","type":"from_role"}]}
  receivers = {$role:a2FuYmFuLWFzc2lzdGFudA}   （base64url "kanban-assistant"）
  session_uri = session://system/socialware-install-kanban/kanban-1294-reverify

## 结论
**#1294 修好了本路径**：同一向导、同一 socialware（kanban，2×cc-headless Fresh），
create_session 由"5s 超时红条 + 后台补建"变为 ~1.14s 同步建成、无红条、
成员/规则/已装 socialware 即时齐全。

## 附带观察（非产品问题）
- agent-browser 的 CDP click 在「创建」按钮上两次未触发 dispatch（log/DB 零
  create_session），改用页面内 JS `btn.click()` 后立即触发且只建了一个会话
  （DB 仅 1 行 session snapshot）。属浏览器自动化工具与该 React 按钮的兼容性
  问题，不影响结论。
