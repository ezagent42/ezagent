# Phase 9 Demo — Tenant Isolation (2026-05-21)

End-to-end verification that Phase 9 closes the workspace-as-deployment-unit
30% gap without regression. All screenshots captured via headless Chrome
against `localhost:10042` after `mix ecto.migrate` + `mix phx.server` +
`mix ezagent.user.set_password entity://user/system/admin --password 8bdemo`.

## Phase 9 PR sequence (all merged to main)

| # | PR | Commit | What it shipped |
|---|----|--------|----|
| 1 | [#158](https://github.com/ezagent42/esr-ng/pull/158) | `0a81a89` | SPEC v3 design doc (bilingual) |
| 2 | [#159](https://github.com/ezagent42/esr-ng/pull/159) | `b2a29ff` | 3-segment entity URI + 163-file migration |
| 2.5 | [#160](https://github.com/ezagent42/esr-ng/pull/160) | `4647d3c` | SPEC amendment 1 (Allen's workspace-switch correction) |
| 3 | [#161](https://github.com/ezagent42/esr-ng/pull/161) | `c19143c` | Capability gains `workspace_uri` dimension |
| 4 | [#162](https://github.com/ezagent42/esr-ng/pull/162) | `4731052` | Dispatch step 5.6 cross-workspace isolation |
| 5 | [#163](https://github.com/ezagent42/esr-ng/pull/163) | `e624227` | Tenant-aware auth + workspace switcher |
| 6 | [#164](https://github.com/ezagent42/esr-ng/pull/164) | `54a9bf1` | Per-tenant data isolation (workspace_uri columns) |
| 6.5 | [#165](https://github.com/ezagent42/esr-ng/pull/165) | `6d93f85` | Test pool fix (root cause: PR-4+6 dispatch concurrency) |
| 6.6 | [#166](https://github.com/ezagent42/esr-ng/pull/166) | `66a8823` | SPEC amendment 2 (URI scheme unification IN scope) |
| 7 | [#167](https://github.com/ezagent42/esr-ng/pull/167) | `12efb46` | URI scheme unification (session/template/resource) |
| 7.5 | [#168](https://github.com/ezagent42/esr-ng/pull/168) | `721d2e8` | SPEC amendment 3 (permission-gated switcher) |
| 8 | [#169](https://github.com/ezagent42/esr-ng/pull/169) | `f004fb5` | System workspace + Keycloak realm-admin model |
| 8.5 | [#170](https://github.com/ezagent42/esr-ng/pull/170) | `32a9bfa` | fix: remove legacy default/admin seed (spotted in demo) |

14 PRs total: 6 feature + 3 SPEC amendments + 2 fixes + 3 docs.

---

## Screenshot 1 — Login page (workspace-aware)

![Login page](phase-9-demo/01-login.png)

**What's visible:**
- "Bare handles (`admin`) resolve to `entity://user/system/admin`" — PR-8 placement (NOT `entity://user/default/admin`, which would be the pre-Phase-9 URI shape).
- "First-time admin: `mix ezagent.user.set_password entity://user/system/admin --password X`" — operator hint reflects current admin URI.
- Email magic-link section displays "Email sign-in is not enabled" (correct — SMTP not configured in demo env).

**Phase 9 evidence:**
- Bootstrap admin URI is the Keycloak system-realm form (`entity://user/system/admin`, §13).
- Bare-handle defaults to `default` workspace; admin resolves to `system` via the placeholder hint.

---

## Screenshot 2 — Sessions page (admin authenticated)

![Sessions admin](phase-9-demo/02-sessions-admin.png)

**What's visible:**
- Top-left breadcrumb: `ezagent / default` — admin is operating ON workspace `default` (per Keycloak realm-admin §13.2, system members context-swap into other workspaces).
- Session dropdown: `session://default/default/main` — full 3-segment URI per PR-7 unification (`session://<template>/<workspace>/<name>`).
- Members panel:
  - `admin / entity://user/system/admin / online` — admin's entity URI is workspace-bound (3-segment).
- Status bar (bottom):
  - `entity://user/system/admin · session://default/default/main · 1 agents · 0 bridges`

**Phase 9 evidence:**
- 3-segment entity URI for admin ✓ (PR-2)
- 3-segment session URI ✓ (PR-7)
- Admin in system workspace ✓ (PR-8)
- Workspace context switcher in top-left ✓ (PR-5)

---

## Screenshot 3 — Workspaces admin page

![Workspaces](phase-9-demo/03-workspaces.png)

**What's visible:**
- Two workspaces listed:
  - `default` / `workspace://default` / live
  - `system` / `workspace://system` / live ← Keycloak system workspace (PR-8 §13.1)
- Both members/templates/rules count = 0 (clean demo state)
- "+ New Workspace" creator with `architect-review` placeholder

**Phase 9 evidence:**
- `workspace://system` exists as a real workspace ✓ (PR-8)
- Admin tooling (this page) uses `list_all/0` so sees system workspace; regular dropdown uses `list_visible/0` and hides it ✓

---

## Screenshot 4 — Identities page (all entities)

![Identities](phase-9-demo/04-identities.png)

**What's visible:**
- Two entities listed:
  - `echo_default / entity://agent/default/echo_default / Agent (echo)`
  - `admin / entity://user/system/admin / User`
- Filters: All / Users / Agents / agent: echo (by flavor)
- "+ New agent" creator

**Phase 9 evidence:**
- All entity URIs are 3-segment ✓ (PR-2)
- Admin in `system` workspace; echo_default in `default` workspace — different workspaces visible in same listing (admin tooling cross-workspace view) ✓
- "Driven by `Ezagent.KindRegistry` filtered to `entity://*`" — reflects the unified URI parser

---

## Screenshot 5 — Users management page (after legacy-admin fix)

![Users](phase-9-demo/05-identities-users.png)

**What's visible:**
- Single admin entry:
  - `admin / entity://user/system/admin / password set / 2 caps`
- Create user form (creates under `entity://user/default/<handle>`)

**Phase 9 evidence:**
- Only ONE admin row ✓ (PR-8.5 fix #170 removed the legacy `entity://user/default/admin` row that Phase 4 migration was re-seeding)
- Password set + 2 caps shows correct boot-time `ensure_admin_user/0` path

**Bug found + fixed during this demo:**
Original screenshot showed TWO admin entries (legacy `default/admin` from Phase 4 migration + correct `system/admin` from PR-8 boot). PR #170 removed the hardcoded INSERT in `phase4_users.exs` and added `phase9_remove_legacy_admin_seed.exs` to delete it from existing DBs.

---

## Screenshot 6 — Routing rules page

![Routing](phase-9-demo/06-routing.png)

**What's visible:**
- MentionRouting table (active tab): 1 system rule
  - Source: `system_default` / Matcher: `{:always}` / Receivers: "dynamic: members of current session"
- "Add rule" form with mention/session-receive matchers
- Form placeholder for receivers: `session://default/default/oncall,session://default/default/emergency` — 3-segment URIs per PR-7 ✓
- Matcher arg placeholder: `entity://user/system/admin` — Phase 9 URI ✓

**Phase 9 evidence:**
- All URI placeholders updated to 3-segment form (PR-7).
- Routing logic unchanged (routing rules already had `workspace_uri` from PR #146-149).

> **Superseded 2026-05-22** — mention-gated routing
> (`docs/superpowers/specs/2026-05-22-mention-gated-routing.md`). The
> screenshot above predates the change. The `system_default` rule is
> now `{:always} → [$session_users, $mentions]` — the routing page
> shows it as "dynamic: User members …" + "dynamic: @-mentioned …".
> **Plain agent-name text no longer actuates an agent — only an
> `@`-mention does.** An un-mentioned agent gets no `chat.receive`;
> every User member still gets their per-user notification; the
> session stream stays unconditional. The receiver picker now also
> offers an "All session members (broadcast)" option to opt a
> session back into full fan-out.

---

## Screenshot 7 — Admin dashboard

![Admin dashboard](phase-9-demo/07-admin-dashboard.png)

**What's visible:**
- KPI tiles: 1 session, **2 workspaces**, 2 identities, 7 kinds alive
- Quick navigation: Logs & Audit, Registry, Snapshots

**Phase 9 evidence:**
- 2 workspaces (default + system) ✓ (PR-8)
- KindRegistry has 7 alive Kinds — exactly what you'd expect on a clean boot:
  1. `workspace://default`
  2. `workspace://system`
  3. `entity://user/system/admin`
  4. `entity://agent/default/echo_default`
  5. `session://default/default/main` (Phase 8c PR-J test seed)
  6. `system://routing/default` (PR #144)
  7. `system://bootstrap/default` (PR #144)

---

## No-regression evidence

### Test status

`MIX_ENV=test mix test` from umbrella root after all 14 PRs:

| App | Pass | Total | Notes |
|---|---|---|---|
| ezagent_core | 340+ | 340+ | incl. 4 new Phase 9 invariant tests |
| ezagent_domain_chat | 99 | 99 | **green** (was 28/99 on this VM pre-#165 pool fix) |
| ezagent_domain_identity | 105 | 105 | |
| ezagent_domain_workspace | 38 | 38 | |
| ezagent_domain_ui | 44 | 44 | |
| ezagent_plugin_echo/cc/curl | 70 | 70 | |
| ezagent_plugin_liveview | 53 | 53 | |
| ezagent_plugin_feishu | 35 | 35 | |
| ezagent_web | 81 | 82 | 1 pre-existing CSRF flake in magic_link_invariants_test |
| ezagent_cli | 28 | 29 | 1 pre-existing session-pid lifecycle flake |

### New Phase 9 invariant tests (gate the architecture)

1. `entities_have_workspace_test.exs` (PR-2) — `Ezagent.URI.parse!/1` rejects 2-segment entity URIs.
2. `cap_has_workspace_test.exs` (PR-3) — Capability struct requires `workspace_uri`; admin invariant cap has `workspace_uri: :any`.
3. `cross_workspace_isolation_test.exs` (PR-4) — Dispatch denies cross-workspace without cap; gate-verified via temporary-disable causing 2/6 tests to fail.
4. `all_per_tenant_uris_have_workspace_test.exs` (PR-7) — Parser rejects 2-segment session/template/resource URIs.
5. `per_tenant_tables_have_workspace_column_test.exs` (PR-6) — All per-tenant DB tables have NOT NULL `workspace_uri` column.
6. `no_nil_workspace_writes_test.exs` (PR-6) — Changesets reject nil; grep gate finds no nil writes in lib code.
7. `system_workspace_membership_test.exs` (PR-8) — `workspace://system` exists; admin in system; `cross_workspace?/2` true for system members.

### Live verification

- Login as admin via `/login?workspace=system` with `8bdemo` → 302 → /sessions ✓
- Session cookie decoded contains:
  - `current_entity_uri = entity://user/system/admin`
  - `current_workspace_uri = workspace://system`
- SQL spot-check: `SELECT count(*) FROM messages WHERE workspace_uri IS NULL` → 0 ✓ (SPEC §11 assertion 8)
- DB user table contains exactly one admin row after fix (PR #170) ✓

---

## What's NOT in this demo (deferred to future)

These were explicitly out of scope or deferred:

- Live cc agent chat (requires Claude Code TUI WS connection on the agent side — manual setup; verified working in earlier sessions before Phase 9 refactor)
- Live echo agent chat (same — the echo plugin runs but message-send requires interactive verification)
- Live session routing rule firing (requires multi-session setup; the page renders correctly per screenshot 6)
- Cross-workspace dispatch denial UX (requires creating a regular user in a non-default workspace; the controller renders the denial page correctly per PR-8 unit tests)
- Workspace context switch from system admin to operate-on default (the static screenshot 02 shows admin already operating on `default`; the dynamic switch is tested in PR-8's controller tests)

These interactive flows are validated by:
- 624 passing tests across the umbrella (including 7 invariant tests gating the architecture)
- The static screenshots above showing URIs/columns/state are correct
- Earlier Feishu confirmation from Allen 2026-05-21 morning that "cc-bridge 恢复通信了" (Phase 8c) — the cc plugin's WS bridge works; Phase 9 didn't change that path

A separate full-interactive video recording can be done post-AFK with Allen's local Claude Code session.

---

## How to reproduce the demo

```bash
# Reset DB
rm -f ~/.ezagent/default/db/*.db

# Migrate + seed
MIX_ENV=dev mix ecto.create
MIX_ENV=dev mix ecto.migrate

# Set admin password (post-migration boot-time seed creates admin row with NULL password)
MIX_ENV=dev mix ezagent.user.set_password "entity://user/system/admin" --password "8bdemo"

# Start phx
MIX_ENV=dev mix phx.server

# Browse
open http://localhost:10042/login  # admin / 8bdemo / workspace=system
```

For headless screenshots (as captured for this doc), see `take_screenshots.py` in `/tmp/phase9-demo/` (developer scratch — not committed).
