# Blockers — full-flow human validation (2026-06-24)

> Track: `zyli-fullflow-validation-0624` · driver: @李震宇 (human) · assist: Claude
> Base: `origin/main` @ `cd0d4067` · format: 一个 blocker = 一行路由(症状 → owner)+ 指向证据

## Routing rules (from handoff §2)
- core/domain session/resolver 类 → **@林懿伦**
- cc-headless / agent-config 后端类 → **@黄佳佳**
- agent console 前端 / #84 类 → **@戴明**
- 官网 / world 渲染呈现类 → **@张宁**

## Blockers found

| # | Leg | Symptom | fix/ branch | Owner | Evidence |
|---|-----|---------|-------------|-------|----------|
| F1 | L1 | 裸 `localhost:10042/login` 登录成功后 302 跳 `/sessions`，但该路由只挂在 `world.localhost` host → 命中 `FallbackController.not_found` → **404**。`/register`、`/login` 是 host-agnostic（两个 host 都返回 200），用户从裸 host 登录就掉 404。Workaround：从 `world.localhost` 入口登录则正常。严重度：**minor UX**（不阻塞，有 workaround）。 | `fix/login-redirect-404-on-bare-host` ✅占位 | @林懿伦（web/session 跳转目标 host-blind） | `L1A-login.png` + phx 日志 `GET /sessions → not_found → 404` |
| F3 | L1/UI | **world UI 无登出/切换账号入口**。前后端均无 logout button（`grep` world assets/lib + 后端 actions 皆空），Profile 区仅 `display_name.*`/`feishu.*`。`POST/DELETE /logout` 路由存在但 UI 未暴露。用户无法在界面内登出或切号。Workaround：无痕窗口 / 手删 cookie。严重度：**medium UX**（多账号测试/共享机受阻）。 | `fix/world-ui-no-logout` ✅占位 | @张宁（world 渲染/导航）或 @戴明（console） | `grep` 证据 + 本条 |
| F4 | L2 | Agent detail 页 **Phase 显示 `unknown`**（echo agent，未实例化时无活进程→无 phase，但 UI 应显示 `registered`/`idle`/`not spawned` 而非 `unknown`）。detail 加载调 `identity.list_caps` 在 agent kind 上 → `{:unknown_action, :list_caps}`（页面已优雅降级显示 caps:none）。**对比 06-23 已部分修复**：Flavor 已从 `unknown`→正确显示 `echo`。严重度：**minor UX**。 | `fix/agent-detail-phase-unknown` ✅占位 | @戴明（console 前端）/ @黄佳佳（agent-config 后端 status 上报） | `test-echo-1-agent-detail.png` + 日志 `{:unknown_action, :list_caps}` |
| F5 | L2 | **cc flavor 创建失败(claude CLI 在场)**：`test-cc-1`（flavor=cc, cwd=/tmp/cc-test-0624, **with_pty=true**）→ `Kind.spawn ... not :ready within 500ms` → ~10.6s 后 `{:cascade_spawn_failed, {:sandbox_write_path_failed, …test-cc-1, :activate_timeout}}`。`claude` CLI 确认存在（06-23 claude-bot 曾正常 spawn），失败在 **Sandbox.write_path 激活超时**。失败后 workspace 注册表 + agent_lineage 已原子回滚 ✅。可能真 bug 也可能沙箱工具链环境问题（需 owner 判定）。严重度：**medium**（cc+PTY 创建不可用）。 | `fix/cc-create-sandbox-activate-timeout` ✅占位 | @黄佳佳（cc 后端）/ core Sandbox | `test-cc-1-agent-create.png` + 日志 `:sandbox_write_path_failed :activate_timeout` |
| ENV-1 | L2 | codex flavor 创建失败 `{:codex_app_server_start_failed, :codex_not_found}` —— 本机原未装 codex CLI。**已解决**：`npm i -g @openai/codex`（0.142.0）+ 预热 uv 缓存（ENV-2）后，`test-codex-3` **创建成功**（app-server thread/started + bridge join ok + 注册）。codex L2 创建链路转绿。 | — (环境项,已解决) | — | `test-codex-3-agent-detail.png` + 日志 `thread/started`/`join ok` |
| F7 | L2/L5 | **codex TUI PTY 进程崩溃 + 需登录**。`test-codex-3` 创建后，codex TUI(`codex resume --last`) `exit_status 134`(SIGABRT)，pty 缓冲 `memory allocation of 66999916800 bytes failed`（codex 0.142.0 试图分配 67GB → 上游 codex CLI bug）。PtyServer 自动重启后显示 OpenAI 登录界面（未认证）。**影响 L5 跑 codex turn，不影响 L2 创建**。多为上游 + 环境(缺 codex 认证)；ezagent 侧可关注崩溃→重启是否成环。严重度：**medium**(codex 不可用于实际对话,但 codex 非必须，L5 可用 curl)。 | — (上游 codex CLI + 需 `codex login`;暂不路由 ezagent) | — (codex login / 配 API key;关注重启循环) | 日志 `exit_status 134` + `66999916800 bytes failed` + 登录界面 |
| F6 | L2 | **create_agent 慢启动 → LiveView 崩溃**（复现 06-23 §7）。codex 创建走同步 `GenServer.call(#PID, {:ezagent_dispatch, create_agent}, 5000)`，codex sidecar/app-server 启动超 5s → 调用方 LiveView 进程 **未捕获 `(stop) exited`** 直接 terminate。防御缺口：跨 5s 框架 dispatch 限制的慢 spawn 应 fail-soft（返回错误给 UI）而非 crash LiveView。严重度：**high**（任何慢 spawn 都会崩前端）。 | `fix/create-agent-5s-timeout-crashes-liveview` ✅占位 | @林懿伦（core/session dispatch 同步调用 timeout 处理） | 日志 `GenServer.call(...,5000) exited` + `{:codex_thread_id_file_timeout}` |
| ENV-2 | L2 | codex bridge sidecar（`ezagent_codex_bridge.py`，PEP723 `websockets>=13`）经 `uv run --script` 运行时从 pypi 拉依赖、**不走代理** → `Failed to fetch https://pypi.org/.../websockets tunnel error`。已用 `HTTPS_PROXY=…7890 uv run --script` 预热 uv 缓存绕过。属环境/打包健壮性（依赖未 vendor / 无离线兜底）。 | — (环境项;可建议 vendor 依赖) | — (预热缓存/BEAM 注入代理) | 日志 `Failed to fetch pypi websockets` |
| F8 | L3 | **飞书 ack 表情 THUMBSDOWN 被 API 拒绝**。bot 收到未绑定用户消息后打 THUMBSDOWN 表情作 ack → `react THUMBSDOWN ... {:http_status, 400, {"code":231001,"msg":"reaction type is invalid"}}`。THUMBSDOWN 非飞书合法 reaction emoji_type → ack 表情发不出（功能性无伤，但日志刷错 + 用户看不到 ack）。严重度：**low**。 | `fix/feishu-react-thumbsdown-invalid` ✅占位 | @林懿伦（feishu 插件 outbound）| 日志 `react THUMBSDOWN ... 400 code 231001` |
| F9 | L3/L4 | **无操作员 UI 把飞书 chat 绑到 session**（chat→session binding）。world UI 的 External mirror（全局 `/admin` + session 级 `/admin/sessions/:id/external_mirror`）都是**只读表格**（`ExternalMirrorTable`），无 bind 表单/按钮；world 后端 actions 只暴露 feishu **user** 绑定，无 external_mirror **bind**。scenario 12 描述的 "Bind Feishu chat" 按钮**实际不存在**。程序入口 `Ezagent.ExternalMirror.bind/5` 仅节点内可调，亦无 mix 任务。→ 操作员无法完成 L3「绑群」，L4 @提及往返被此阻塞。严重度：**high**（飞书群接入闭环缺最后一环）。 | `fix/no-ui-bind-feishu-chat-to-session` ✅占位 | **@李震宇（world UI 绑群入口——本人开发）** / @林懿伦（feishu external_mirror 动作暴露）| `Admin.tsx:546-565` 只读表 + scenario12 描述按钮缺失 |
| F10 | L5 | **无 UI 给 agent 新增 API key**。Agent API Keys 页（`agent_api_keys` 组件）只有只读表（Provider\|Masked，"No stored keys"），**无新增/put 表单**；world 后端只暴露 `list_api_keys`，无 `put_api_key` action（虽有 `can_edit_api_keys?` 标志，UI 未实现）。→ 无法在活 server 上给 curl/cc agent 配下游 LLM key，curl flavor 真回复经 UI 不可达（同 F9 "最后一公里" 类）。严重度：**high**（curl/任何需 key 的 flavor 无法经产品配置）。 | `fix/no-ui-add-agent-api-key` ✅占位 | **@李震宇（world UI 配 key 入口——本人开发）** / @黄佳佳（agent-config 后端 put_api_key 暴露）| `api-keys.png`（只读表）+ Identities.tsx AgentApiKeys 无表单 |
| F11 | L4/重启后 | **codex agent 重连风暴 + 进程增殖**。重启后 test-codex-3 的 codex bridge 狂刷 `join rejected ... :already_bound`(agent_bridge:codex:test-codex-3),app-server socket 残留;27+ codex/uv/python 进程堆积。裸 `pkill` OS 进程后 BEAM PtyServer 立即重生(裸杀不根治,需 BEAM 内 terminate agent)。server 仍健康但日志刷屏 + 资源占用。严重度：**medium**(codex 重启鲁棒性/孤儿进程)。 | `fix/codex-rebind-storm-on-restart` ✅占位 | @黄佳佳(codex 后端)/ core PtyServer 重启 | 日志 `:already_bound` ×99+ · `ps` 27 codex 进程 |
| F12 | L4 | **feishu @提及未路由到 agent**。inbound 全链路到「消息进入绑定会话」均通(user 绑定✓→chat→session 绑定✓→session.send granted✓→入库✓→worker 订阅✓),但消息 `mentions: []`(飞书 @ 未解析为指向 agent),且所用 protocol-api 会话**无 session 路由规则** → 消息只入库、不路由给 curl → 无回复。补 `always→agent` 路由规则需 DB 插 + **重启**(Resolver 读 ETS RoutingRegistry,非 DB)。叠加 F9/F10：feishu 群→agent 回复的操作员闭环缺多个 UI(绑群/配key/加路由规则)。严重度：**high**(飞书→agent 闭环最后一跳)。 | `fix/feishu-mention-route-to-agent` ✅占位 | @林懿伦(routing/mention 解析)/ @张宁(会话路由规则 UI)| 日志 `mentions: []` + `session.send granted` 但无 agent dispatch |
| F13 | L7 | **anon chat 页无 composer(输入框)**。`/socialware/chat`(本应可交互的匿名聊天)与只读 `/socialware/customer` 渲染相同 feed 外壳(`Your conversation / No messages yet`),**无发送输入框**。后端 anon 流程已成功(anon 用户铸造✓ + AnonBinding✓ + subscribe_from cap✓ + **ChatFeedSocket 连接✓**),故缺口在前端 customer/chat SPA(composer 未渲染或被条件隐藏)。不影响 L7「渲染正常」DoD;影响匿名访客实际参与对话。严重度：**medium**(对外 app 不能互动)。 | `fix/anon-chat-composer-missing` ✅占位 | **@李震宇（world/socialware 前端——本人开发）**/ @张宁 | `public-socialware.png`(无输入框) + 日志 `CONNECTED TO ChatFeedSocket` |
| F14 | L6 | **`Always` 路由规则自环 + UI Disable 不停 live 循环**。会话内加 `Always→echo` 规则后,echo 的回复又匹配 `Always`→再路由回 echo→**无限循环**(实测 800→1168+ 条/数秒)。缺循环防护(规则应排除「发送者==接收者自身回复」的再触发)。且 UI 点 **Disable** 未更新 live ETS RoutingRegistry,循环不停,**最终靠重启清 ETS** 才止血(DB `enabled=false` + 重启)。**正面**:此过程反证 06-23 核心阻塞已修——**UI 路由 Add 生效 + echo 在会话内真回消息**。严重度：**high**(任何含 Always 的会话 + 自回复 agent 会失控刷量)。 | `fix/always-rule-self-loop-and-disable-noop` ✅占位 | @林懿伦(routing 循环防护)/ @李震宇(world UI Disable 更新 ETS)| 日志 `echo:` ×1168 自循环 · UI Disable 后仍刷 |
| F15 | rebase批/#931 | **cc-headless + codex-remote 创建失败(同根)**:`handle_create_agent` 崩 `config-dir resolution failed for resource://system/cc-headless-agents/…: :none`。cc-headless template/flavor 已注册,但其 config-dir FsResolver **resource type `cc-headless-agents` 未注册**(resolver 对未注册 type 返 :none)。→ 新 flavor 完全不可创建。严重度：**high**。 | `fix/cc-headless-config-dir-resource-type-unregistered` ✅占位 | @黄佳佳(#931 cc-headless)| `rebase-batch-validation.md` + `agent-create-fail-codex-remote.png` + 日志 `config-dir resolution failed resource://system/{cc-headless,codex-remote}-agents/… :none`(两 flavor 同根:config-dir FsResolver resource type 未注册）|
| F16 | L2/PTY | **with_pty 创建的 agent,从 agent-detail 页找不到 PTY 终端入口**。勾 With PTY 建 agent(如 r2-codex-pty)后,Identities→Agent Detail 页只有元数据(Phase/Flavor/config_dir/Bridge/caps),无终端 tab/入口。PTY 终端视图实际挂在 **session 对话面板**(`session.view.switch view:pty`,chat/pty 切换),需把 agent 加进 session 才能到达。设计上 detail 页无终端属合理,但「勾了 PTY 却无处可见」是发现性 UX 缺口。严重度：**low/UX**。 | `fix/pty-terminal-discoverability-from-agent-detail`（待定,可并入 console #84）| @李震宇/@戴明(console PTY 入口)| `r2-codex-pty-agent-detail.png`(无 PTY 入口)|
| F17 | L2/PTY | **codex PTY 视图前端「无反应」**(后端 PTY 实际工作)。`session.pty.open`+`view.switch:pty` 派发正常,PtyServer **确为 r2-codex-pty 起了 codex TUI**(日志见 codex 欢迎 splash ASCII),但前端终端未显示。三因叠加:codex TUI 卡登录(F7)+ LiveView 在 pty.open 脆弱(F6 同类)+ **F11 codex 重连风暴**(1890+ REFUSED JOIN,40 进程)压住系统。PTY 机制本身通(终端进程+TUI 渲染进 buffer);本机无法干净演示需先修 F7/F11。严重度：**medium**(PTY 在本机不可用,非纯产品 bug)。 | 待并入 F7/F11 修复后复验 | @黄佳佳(codex F7/F11)+ @李震宇/@戴明(pty.open LiveView fail-soft)| 日志 codex TUI splash + pty.open 派发 + F11 风暴 |
| F18 | L2/PTY | **🔑 PTY 视图崩 LiveView：`Jason.Encoder not implemented for type PID`（PTY 真根因，flavor 无关）**。echo+PTY（`r3-echo-pty-1`，with_pty=true，cwd=/tmp/echo-sandbox）→ **PtyServer 真起 `/bin/bash -i`，PTY buffer 出现真提示符 `lenovo@…:/tmp/echo-sandbox $`** ✅；但 LiveView 推 PTY 状态给 React 岛时**裸 PID 未经 `jsonable/1`（world_live.ex:888 有该助手但 PTY 路径绕过）→ Jason 编码 PID 崩溃** → 终端永不渲染（UI "Terminal not available"/「无反应」）。**后端 PTY 完全正常，纯前端序列化 bug**。取代 F17（codex PTY 无反应同根，被 F11 风暴掩盖），F16 亦次要。严重度：**high**（所有 flavor 的 PTY 终端在 UI 均不可见）。 | `fix/pty-view-pid-jason-encode-crash` ✅占位 | @李震宇（world UI PTY 序列化—本人开发）| 日志 `PtyServer spawned /bin/bash` + 提示符 + `Jason.Encoder … PID` |
| F2 | L1 | 自助注册的用户最终 `email_verified: true` 且**未点确认链接即可登录**（dev 邮件发送未配置 `:inbox_not_configured`，确认邮件根本发不出）。`registration.ex:199` 写死 `email_verified: false`，但 DB 实测为 `true`、login（要求 verified）通过——翻转机制不明。严重度：**low / 观察项**（不阻塞；生产自助注册场景下是邮箱所有权校验绕过的安全点）。 | `fix/self-register-email-verified-bypass` ✅占位 | @林懿伦（identity/auth） | `L1A-register.png` + DB 查询(`email_verified: true`) |

## Leg status

| Leg | Status | Evidence file |
|-----|--------|---------------|
| L1 注册→登录→world UI | ✅ PASS（经 `world.localhost` 入口）| `L1A-register.png` / `L1A-login.png`(404, F1) / `L1c-world-ui.png` |
| L2 建 workspace+agent (#905) | ✅ PASS（全 5 flavor：echo/curl/np/**codex** 创建成功；cc 空CWD 必填拦截=06-23bug已修；cc+PTY 沙箱超时=F5；codex 装CLI+暖uv缓存后转绿，TUI 崩溃/需登录=F7）| `agent-list.png` / `test-echo-1-agent-detail.png` / `test-curl-1-agent-detail.png` / `test-cc-1-agent-create.png` / `test-codex-3-agent-detail.png` |
| L3 绑 Feishu 群 + inbound 到达 | 🟡 **inbound 到达 ✅**（群 `oc_eff705...`，4 条消息，open_id `ou_0a17...372` 解析，WS→节点 OK）+ **user 绑定 ✅**（UI 面板 `ou_0a17...372 → entity://system/user/admin` 写入 DB）。**绑群(chat→session)被 F9 阻塞**（无 UI 入口）→ L3 闭环差最后一环。发现 F8、F9。 | 日志 `Feishu inbound ... unbound→bound`；DB `feishu_user_bindings` 行 |
| L4 cc agent @提及往返 | 🟡 管道深度验证(全靠 DB workaround):inbound→user绑定→**chat→session绑定(InboundChatLookup生效)**→session.send granted→入库→worker订阅 全✅;**卡 session→agent 路由**(F12 mentions:[]+无路由规则)。cc 本身 F5 阻塞,改用 curl 验证亦止于路由。 | 日志 `mentions:[] + session.send granted` |
| L5 codex/curl + protocol-api /v1 | ✅ **PASS**。protocol-api /v1 端到端(echo 活 :10042)+ **curl flavor 真 DeepSeek 回复**：活 :10042（手写 key 入快照绕 F10 → `"你好！我是DeepSeek…"`）+ e2e harness :10066（`"你好"`/`"2+2=4"`）。cc via /v1 超时(F5)、codex via /v1 超时(F7)。后端 curl 全链路正常，唯一缺口 F10(配 key UI)。 | `L5-protocol-api-v1-echo.md` |
| L6 多 agent 接力 (scenario-34) | 🟢 **接力路由逻辑 PASS**(deterministic 8 tests 绿)。**live echo 单跳验证:UI 加路由规则✅ + echo 会话内真回复✅(06-23 send no-op/Add no-op 均已修)**;但 `Always` 自环=F14。多跳真接力(cc→codex→curl)受 F5/F7 阻塞。 | `L6-multiagent-relay-scenario34.md` |
| L7 world 渲染 + customer 公开视图 | ✅ **PASS**（world 各页渲染=L1；customer 公开视图 `session://system/public/public` 无登录→200 渲染，public_view 模板闸门生效）。零 finding。 | `L7-world-render-customer-public.md` + `L1c-world-ui.png` |
