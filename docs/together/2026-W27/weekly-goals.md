# Weekly goals - 2026-W27 (Mon 2026-06-29 -> Sun 2026-07-05)

The week's goals. Each day's `plan.md` tracks must ladder up to one of these.

1. **Get ezagent running inside the team** - the product works end-to-end well
   enough that the team uses it daily. This week the blocking path is: finish
   AutoService live bootstrap, close the deploy/world-host routing gaps, keep
   `precommit + check_invariants` green, and convert QA findings into merged
   fixes rather than loose reports.
2. **Build the official website** - keep the website/demo work on the
   `@json-render` render substrate while avoiding collisions with operator
   World/socialware surfaces.
3. **Make dev-together self-bootstrapping** - close stale team PRs, keep daily
   plan/stack/review ledgers complete, and turn #1020 kanban/dev-together E2E
   into a reviewed, mergeable slice or an explicit deferred backlog item.
