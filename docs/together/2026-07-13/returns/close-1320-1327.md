# Return — consolidate and close PR #1320 / #1327

> **Task:** carry the valid #1320 and #1327 changes onto current `main`
> **Branch:** `codex/close-1320-1327`
> **PR:** https://github.com/ezagent42/ezagent/pull/1365
> **Dev:** Codex（zyli-developer 席位）
> **returned_at:** 2026-07-13 +0800

## Outcome

- Created a fresh branch from `origin/main` and carried forward the class-template session-listing fix from #1320.
- Carried forward the socialware-uninstall browser evidence and canonical return update from #1327.
- Kept the session facade within the current architecture budget by exposing `join_session_members/2` as a thin `defdelegate` to the materializer.
- Superseding PR: #1365. The old PR branches are not rewritten.

## Validation

- `mix format --check-formatted` — pass.
- `mix compile --warnings-as-errors --force` — pass.
- `mix ezagent.arch.scan` — pass; `def_count_session_creator` is 32 with cap 34.
- `POSTGRES_HOST=127.0.0.1 POSTGRES_PORT=5432 mix test apps/ezagent_domain_session/test/integration/generic_session_create_via_workspace_test.exs` — 2 tests, 0 failures.
- `POSTGRES_HOST=127.0.0.1 POSTGRES_PORT=5432 mix test apps/ezagent_web/test/ezagent_web/world_conversation_test.exs:641` — 41 tests, 0 failures, 40 excluded.
- `POSTGRES_HOST=127.0.0.1 POSTGRES_PORT=5432 mix precommit` — pass, exit 0, 528.4 seconds.

The WSL test environment used PostgreSQL 17 client tools from the host and the development cluster on `127.0.0.1:5432`. The first full run exposed the missing WSL `pg_dump` command; after making the host `pg_dump`/`pg_restore` available under a temporary WSL PATH, the failed migration tests and the complete precommit gate passed.

## Remote gate

At the pre-document head of PR #1365:

- `gate (deterministic)` — success.
- `gitleaks secret scan` — success.
- `Return file advisory` — success.
- `Only repo owner may edit dev-together skill` — success.
- `full-suite (self-hosted macOS)` — skipped while the PR is a draft.

## Handoff

PR #1365 is the single current-main integration point for both old PRs. Keep it as the review target; #1320 and #1327 can be closed as superseded once the lead accepts #1365. No direct merge was performed.
