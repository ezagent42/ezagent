# MFU 成长树 · Living Doc

> **状态**: Living · v0.15 成长树规则；v0.14 节点保留为迁移台账
> **创建**: 2026-07-24（原 `doc/skill-trees.md`）· **迁入本目录**: 2026-07-27
> **定位**: 成长树的**单一事实源**——能力分类、达标节点、图形关系、问题登记、SOP 与路线图全部在此迭代。
> **规则**: GDD 与审计文档是版本快照，不回改；快照与本文件冲突时以本文件为准，并在 §9 变更记录中说明。改树必改本文。与其他文档的关系见附录 A。

> 平台全部概念及关系以 [`platform-concept-model.md`](platform-concept-model.md) 为准。本文不再承担经验、认证、会籍与平台功能的定义。

**术语更新**：v0.15 统一使用「成长树」。技能树、科技树和 `TREE_NODES` 是历史名称。成长树可以进阶、并列分叉、汇合或交叉，不要求是严格的单根树。

---

## 1. What —— 成长树是什么

**成长树只展示已经被证据证明的稳定能力，不是经验条、徽章架或平台功能菜单。**

节点必须同时满足：

1. 表达“已经能够完成什么”，而不是“正在学习”；
2. 离开 MFU 平台后仍然成立；
3. 有明确的任务与评价方式；
4. 粒度足够稳定，不把单一工具、按钮或作品形态误当成完整能力。

### 1.1 三类成长树

| 成长树 | 谁拥有 | 展示什么 | 不展示什么 |
|---|---|---|---|
| 个人成长树 | 主理人 | OPC 核心能力与专业能力的达标结果 | 打工/作品计数、信誉等级、会籍、功能按钮 |
| 公司成长树 | 公司 | 可稳定复用的组织能力 | 主理人技能与 Agent 能力的简单并集 |
| 角色成长树 | 已取得角色资质的个人或组织 | 在教练、孵化、平台运营等角色中的实践能力 | 角色资质本身、工作台按钮 |

### 1.2 v0.15 个人成长树

OPC 核心能力采用六项最小集合：

- 机会判断；
- 客户理解；
- 方案设计；
- 资源编排；
- 交付负责；
- 复盘迭代。

专业领域允许放射状展开。例如“内容与表达”是分类，下面可以并列图文、视频、长文等方向；分类本身不是达标节点。

每个方向可以出现三类可评价成果：

- 基础应用达标；
- 独立交付达标；
- 复杂项目达标。

专业评审、教学带教、方法创新和专业负责是复杂项目之后可能出现的不同分支，不是统一的第四级。

---

## 2. Why —— 为什么这样设计

### 2.1 解决什么问题

成长树模型延续 flywheel-audit 对 progression 的要求，但不再承担全部权限：

| 问题 | 成长树如何回应 |
|---|---|
| Progression 有骨架无感知 | 树**就是**骨架：节点 = 里程碑，解锁 = 有仪式感的事件，分支 = 可选路径 |
| 白银→黄金中期真空 | 能力证据持续积累，学生每周都能看到距下一项达标还差什么 |
| 四种 Bartle 玩家类型出口单一 | 不同能力分支、专业方向、角色实践与真实挑战提供不同追求 |
| 其他角色界面要不要做 | 角色资质负责开通工作台，角色成长树负责表达开通后的成长 |
| 目标透明度 | 能力标准与本人证据公开透明；孵化器偏好和最终选择不由平台预设 |

### 2.2 核心模型与兑现链

```
课程 / 任务 / 项目
        ↓
经过评价且标明贡献归属的能力证据
        ↓
满足平台统一能力标准
        ↓
能力节点达标
```

能力达标可以成为平台功能开放的条件之一，但功能本身不进入成长树：

```text
能力 + 信誉等级 + 专业认证/角色资质 + 会籍/人工审批
        ↓
独立的功能开放规则
        ↓
联合竞标、公开评审、工作台等平台功能
```

### 2.3 设计决策记录

> D31–D33 来自 GDD v0.14 §0.1（快照）；D35–D41 于 2026-07-27 在本文档订立（待写入下一版 GDD）。D34 仍是开放问题（GDD v0.14 #12：企业树是否=成员个人树并集；D39 给出倾向回答「否」，Allen 已认可框架方向）。

| # | 决策 | 要点 | 状态 |
|---|---|---|---|
| D31 | 动词规则 | 节点不含纯数值加成。每个节点解锁一个新**动词**；动词在整棵树上只出现一次 | 已定 |
| D32 | 公司树 = 学分线 | 公司树节点仅由学分积累 + 资格认证解锁。打工是生存手段，不推进公司树 | 已定 |
| D33 | 统一起点 | 所有组织初始 = 学生企业，共享同一基础面板与主干树；差异 = 各自开启了哪些资质树。**注：原表述「单棵树多分支」被 D35 修订，内核（统一起点）保留** | 已修订 |
| D35 | 多树结构 | 单主干 + 资质树森林。主干树（企业经营）、个人树（经历线）、资质树（角色树，由认证节点开启）。认证节点同节点兼作资质树根 | **已定 2026-07-27** |
| D36 | 认证双重记录 | 白银/黄金等认证：公司获得认证（影响业务、官网展示），个人档案同步记一笔（跨公司保留）。个人树节点可用个人认证记录做条件 | **已定 2026-07-27**（demo 已落地 `S.profile.certs.gold`） |
| ~~D37~~ | ~~打工仅解锁个人树~~ | **已被 P-15 框架取代（Allen 批准）**：打工只进经验条（活跃度），不解锁任何技能树节点 | 已取代 |
| D38 | 树内容平台统一定义 | 所有树（含资质树）的结构与数值由平台运营（=策划）定义；角色组织只有解锁与使用权，可经协会渠道提案 | **已定 2026-07-27** |
| D39 | 成长归属 = 资产归属 | Agent 的成长属于其所有者实体（公司资产，D19），不计入主理人个人成长；Agent 不设成长树（Agent 是资产不是实体，progression = 能力分 + 资产清单 + 里程碑徽章）；0 号 Agent（小 H，D29）属个人，其沉淀为个人偏好配置资产。对 GDD v0.14 开放问题 #12（D34）倾向「否」 | **已定 2026-07-27**（框架方向 Allen 已认可） |
| D40 | 学分代理（demo 临时） | 练习单重新定义为「课程作业」：只有质量过门的完成才计学分（demo 质量门 = 班内竞赛排名前 50%，即 4 队竞赛的前 2 名）；未来接入学校教务系统的真实学分。与 GDD v0.13 §5.4「学校培训」一致 | **已定 2026-07-27**（demo 已落地） |
| D41 | 外部认证录入 | 主理人/公司可录入平台外获得的认证资格（学历、行业证书、作品集奖项），经核验后解锁相应技能树节点——解决「高手新入驻」场景（B 案例：平台外水平高但无平台记录）。核验机制待定（协会认证 Agent 初审 + 人工抽查？）；录入的外部认证在徽章架上与平台内认证并列展示、标注来源 | **已定 2026-07-27**（Allen 批准框架时确认的方向） |
| D42 | 成长树不等于功能菜单 | 联合竞标、资产出租、公开评审、A/B 测试器和各类工作台属于平台功能；能力、信誉、认证、资质等只作为功能开放条件 | **已定 2026-07-28** |
| D43 | 节点必须是达标结果 | 节点统一表达可评价的完成状态，不使用“学习中”；过程通过未开始/证据积累中/已达标展示 | **已定 2026-07-28** |
| D44 | 宽领域 + 分支能力 | 不再把 A/B 实验、模板等细碎方法各做节点。领域用于分类，能力成果可以进阶、并列、汇合和交叉 | **已定 2026-07-28** |
| D45 | 多校课程映射 | 平台统一能力标准，学校自由设计课程；课程与能力是多对多关系。学分/学时是证据输入，不直接证明能力 | **已定 2026-07-28** |
| D46 | 证据必须标明贡献归属 | 学生主导、Agent 主导、共同完成分别记录；只有学生承担关键判断的部分才进入个人能力证据 | **已定 2026-07-28** |
| D47 | 三类成长树 | 保留个人、公司、角色三类成长树；角色资质先开通工作台，角色成长树描述履职后的成长，资质本身不作节点 | **已定 2026-07-28** |

### 2.4 其他设计原则

- **统一起点**（flywheel-audit §5.2）：做孵化器视图 = 做同一张面板 + 孵化器树解锁的额外模块，不是另做一套界面（D27）
- **树公开**：树结构与解锁状态对所有登录用户公开（D28）——野心机制 + 信任机制 + 竞争情报
- **分支价值由市场决定**（命题供给/会籍需求），系统不标「最优路径」
- **GDKeys 7 原则**的逐条落地见 GDD v0.14 §5.10.4（若落地方式变更，改记到本文 §9）

---

## 3. How —— 成长树的完整逻辑

> **迁移说明**：本节至 §5 记录 v0.14 `TREE_NODES` 的实现事实，用于 v0.15 改造与回归检查，不再定义当前概念。当前成长树规则以 §1、§2 和 `platform-concept-model.md` 为准。

### 3.0 v0.14 节点迁移表

| v0.14 内容 | v0.15 归属 |
|---|---|
| N-00 公司注册 | 基础身份与面板开通，不是能力节点 |
| N-01/N-02/N-03 设计、文案、增长专精 | 改为专业能力领域与可评价的达标成果 |
| N-04 设计提案 | “商业提案”能力证据 + “发起提案”平台功能 |
| N-05 品牌模板 | “方法沉淀与复用”能力证据 + 模板资产 |
| N-06 A/B 实验 | “实验验证”能力证据 + A/B 测试平台功能 |
| N-07/N-11 白银、黄金 | 信誉等级，移入徽章架 |
| N-08/N-09/N-10 联合竞标、资产出租、公开评审 | 平台功能，移入功能开放面板 |
| N-12/N-13/N-14 教练、孵化、平台运营 | 角色资质/内部授权；获得后开通角色工作台 |
| P-00~P-03 打工、作品、人脉、复盘 | 个人经验与履历 |
| P-04 判断力认证 | MFU 课程专业认证，移入徽章架 |
| P-05 孵化器背书 | 荣誉与背书 |
| P-06 创业导师 | 角色身份；是否取得导师资质单独记录 |

### 3.1 树的种类与诞生时机

| 树 | 谁拥有 | 诞生时机 | 访问路径 |
|---|---|---|---|
| 主干树（企业经营树） | 每家公司一棵 | **公司注册时**：N-00 公司注册解锁 = 树诞生 | ERP → 🌳 成长树 tab |
| 个人树（经历线） | 每个主理人一棵 | **主理人建档时**（进入游戏即存在）；P-00 随首次打工解锁 | 主理人档案 → 内嵌 |
| 资质树（角色树） | 获得对应资质的公司 | **认证节点解锁时**：根节点解锁 = 树诞生 = 功能面板开放 | 对应功能面板内嵌（如孵化器工作台） |

已规划的资质树：

| 根节点（主干树上） | 资质树 | 开启的面板 | 子节点内容（草案） |
|---|---|---|---|
| N-12 教练认证 | 教练树 | 教练视图 | 学生企业数据面板、周复盘模板、带队指数 |
| N-13 孵化认证 | 孵化器树 | 孵化器工作台 | Scouting 配置、命题管理、grant/桥贷、投后管理（§8.2） |
| N-14 平台运营 | 平台运营树 | 平台治理面板 | 平台配置、Connector 生态管理、系统健康、目标管理 |

### 3.2 节点的解锁时机与检查机制

- **检查时机**（当前实现，两处）：① `tick()` 周结算——新解锁记入日志（正式版应改为结算日事件推送）；② `renderRadialTree()` 渲染时静默刷新
- **父节点检查自动派生**：节点的 `deps` 声明结构依赖，`nodeAvail(n)` 统一做「所有 deps 已解锁 + 自身条件」判断——`condFn` 只写额外条件，不再手写父节点检查（P-01，2026-07-27 落地）
- **解锁即永久**：已解锁节点不回退，切换专精线已训 Agent 不降级（GDKeys 5：成本是时间，不是损失）
- **三种状态**：`unlocked`（已解锁）/ `available`（条件已满足待解锁）/ `locked`（条件未满足）

### 3.3 条件的合法来源（D32 + P-15 框架）

| 树 | condFn 允许读取的字段 | 打工能否作为条件 |
|---|---|---|
| 主干树（公司树） | 公司字段（`S.credits`、`S.lines`、`S.rep`、`S.cred`、`S.rank`、`S.realN/Q`、公司树节点状态） | ❌ 永不（D32） |
| 个人树 | 个人字段（`S.profile.*`——含 `gigs`、`guildHist`、`certs`、`S.reviews`） | ❌（P-15 批准：打工只进经验条）⚠️ 现有 P-00 条件待框架落地时移除 |
| 资质树 | 同主干树（公司字段）+ 资质树自身节点状态 | ❌ |

**约束**：个人树节点的 `condFn` 不允许读公司状态（`S.guilds`、`S.rank`、`S.cash`…）。认证要通过 D36 的个人认证记录进入个人树。

### 3.4 解锁后兑现什么（grants 机制）

每个节点声明 `grants`（它开启什么）。兑现链：

```
资质认证（如 N-13 解锁）→ 资质树诞生 → 根节点开启功能面板（如孵化器工作台）
                                    → 子节点开启或改变面板内的使用权限
```

- 根节点 grants：`panel:incubator`（面板可见 + 基础只读）
- 子节点 grants：`feature:scouting`（配置候选追踪）、`feature:directed-order`（定向命题）……
- UI 层查询「当前组织已解锁的 grants 集合」决定面板可见性/可操作性
- 主干树节点同理：N-04 设计提案 grants `feature:reverse-pitch`，N-05 品牌模板 grants `feature:template-market`……（当前全部未实现，见 §6 P-08）

### 3.5 树的消亡

| 事件 | 影响 |
|---|---|
| 公司破产/清算 | 公司主干树随公司消亡；**个人树不受影响**（跨公司保留）；公司已获得的认证在个人档案中的记录保留（D36） |
| 公司消亡时的资质树 | 资质树（功能面板使用权）随公司消亡而关闭；个人档案中的认证记录保留，作为重建时的凭据（推论，若需调整在此修订） |
| 个人树 | 永不消亡 |

### 3.6 成长归属：Agent 的成长算谁的（D39）

- **Agent 的成长不算个人成长**。Agent 是公司资产（D19：可出售/转让/归档）——若 Agent 成长计入个人成长，卖掉一个训练好的 Agent 就等于卖掉个人成长，花钱买强 Agent 就等于花钱买个人技能（撞 D17「金钱买不到能力」）
- **Agent 没有成长树**。平台上只有两种实体有树：公司（Org）和人。Agent 是资产，不是实体。Agent 的 progression 用**能力分 + 训练资产清单 + 里程碑徽章**（能力分 6/8/10 觉醒点，见 flywheel-audit C.5）表达，不设树
- Agent 能力分 = f（训练资产， 实战**评价**史）——本就是质量信号驱动的成长，与技能/经验框架（P-15）的原则一致
- **例外**：0 号 Agent 小 H 属于人（D29，随档案永续、不可交易），其沉淀 = 个人偏好配置，算个人资产，但仍不是树节点
- **对 GDD v0.14 开放问题 #12（D34）的回答**：企业树 ≠ 成员/Agent 个人树的并集——倾向「否」，框架方向已获 Allen 认可

---

## 4. 节点定义

> 本节的 condFn 反映**当前代码事实**（2026-07-27 修复包已同步）。「功能兑现」列 = 该节点按动词规则应该解锁的能力（grants），是目标 2 的填写处。
> ⚠️ P-15/P-16 已批准、待落地：N-07/N-11 将移出树（认证徽章架），P-00~P-03 将移出个人树（经验条），见 §8.1。

### 4.1 主干树（公司成长树，scope: `'company'`）

#### N-00 · 公司注册

| 字段 | 值 |
|---|---|
| Tier | 0（根） |
| Verb | 注册 |
| Icon | 🏢 |
| Group | base |
| Deps | — |
| Cond | 进入游戏即解锁 |
| condFn | `() => S.started` |
| 功能兑现 | ✅ 已有：基础面板（Agent/订单/现金流） |

#### N-01 · 设计专精

| 字段 | 值 |
|---|---|
| Tier | 1 |
| Verb | 调教 |
| Icon | 🎨 |
| Group | specialty |
| Deps | N-00 |
| Cond | 设计课程作业 ≥ 5（竞赛前 50% 才计，D40） |
| condFn | `() => S.credits.design >= 5` |
| 功能兑现 | ❌ 应为：设计 Agent 范例库、设计命题适配星级更准（GDD §5.10.3） |

#### N-02 · 文案专精

| 字段 | 值 |
|---|---|
| Tier | 1 |
| Verb | 编写 |
| Icon | ✍️ |
| Group | specialty |
| Deps | N-00 |
| Cond | 文案课程作业 ≥ 5（竞赛前 50% 才计，D40） |
| condFn | `() => S.credits.copy >= 5` |
| 功能兑现 | ❌ 应为：品牌语气模板、模板保存复用 |

#### N-03 · 增长专精

| 字段 | 值 |
|---|---|
| Tier | 1 |
| Verb | 投放 |
| Icon | 📈 |
| Group | specialty |
| Deps | N-00 |
| Cond | 增长课程作业 ≥ 5（竞赛前 50% 才计，D40） |
| condFn | `() => S.credits.growth >= 5` |
| 功能兑现 | ❌ 应为：A/B 测试模拟器 |

#### N-04 · 设计提案

| 字段 | 值 |
|---|---|
| Tier | 2 |
| Verb | 提案 |
| Icon | 📋 |
| Group | deepen |
| Deps | N-01（父节点检查由 nodeAvail 自动派生） |
| Cond | 设计专精 + 真单 ≥ 3 |
| condFn | `() => S.lines.design >= 3` |
| 功能兑现 | ❌ 应为：主动向孵化器发起设计提案（反向 pitch），不等命题发布 |

#### N-05 · 品牌模板

| 字段 | 值 |
|---|---|
| Tier | 2 |
| Verb | 复用 |
| Icon | 📄 |
| Group | deepen |
| Deps | N-02（自动派生） |
| Cond | 文案专精 + 真单 ≥ 3 |
| condFn | `() => S.lines.copy >= 3` |
| 功能兑现 | ❌ 应为：品牌文案存为模板、资产市场出售/license |

#### N-06 · A/B实验

| 字段 | 值 |
|---|---|
| Tier | 2 |
| Verb | 实验 |
| Icon | 🧪 |
| Group | deepen |
| Deps | N-03（自动派生） |
| Cond | 增长专精 + 真单 ≥ 3 |
| condFn | `() => S.lines.growth >= 3` |
| 功能兑现 | ❌ 应为：同一命题提交两套方案取优（风险对冲） |

#### N-07 · 白银认证

| 字段 | 值 |
|---|---|
| Tier | 2.5（资质门） |
| Verb | 入会 |
| Icon | 🥈 |
| Group | gate |
| Deps | —（无依赖，独立解锁） |
| Cond | 声望 ≥ 30 + 信用 ≥ 70 |
| condFn | `() => S.rep >= 30 && S.cred >= 70` |
| 功能兑现 | ✅ 部分已有：预备营/协会会籍准入、真单准入；❌ 缺：Scouting 开始追踪的信号 |
| ⚠️ | P-16 已批准：将移出树，进入认证徽章架；其条件成为「白银认证状态」供其他节点引用 |

#### N-08 · 联合竞标

| 字段 | 值 |
|---|---|
| Tier | 3 |
| Verb | 联合 |
| Icon | 🤝 |
| Group | cross |
| Deps | N-07（自动派生）⚠️ P-16 落地时将重锚到专精线 |
| Cond | 白银 + ≥ 2 条 T1 线 |
| condFn | `() => {const t1 = ['N-01','N-02','N-03']; return t1.filter(id => S.treeNodes[id] && S.treeNodes[id].unlocked).length >= 2;}` |
| 功能兑现 | ❌ 应为：邀请其他 OPC 组队竞标（联机功能，单机版需 NPC 队友方案） |

#### N-09 · 资产出租

| 字段 | 值 |
|---|---|
| Tier | 3 |
| Verb | 出租 |
| Icon | 🏷️ |
| Group | cross |
| Deps | N-07（自动派生）⚠️ 同上 |
| Cond | 白银 + 任一 T2 |
| condFn | `() => ['N-04','N-05','N-06'].some(id => S.treeNodes[id] && S.treeNodes[id].unlocked)` |
| 功能兑现 | ❌ 应为：Agent 授权其他 OPC 使用、按周收 license 费 |

#### N-10 · 公开评审

| 字段 | 值 |
|---|---|
| Tier | 3 |
| Verb | 评审 |
| Icon | 🔍 |
| Group | cross |
| Deps | N-07（自动派生）⚠️ 同上 |
| Cond | 白银 + 作品集 ≥ 10 |
| condFn | `() => S.profile.portfolio >= 10` |
| 功能兑现 | ❌ 应为：对其他 OPC 交付物发起公开评审、计入评审员资格积累 |

#### N-11 · 黄金认证

| 字段 | 值 |
|---|---|
| Tier | 3.5（角色） |
| Verb | 入驻 |
| Icon | 🥇 |
| Group | role |
| Deps | N-07（自动派生） |
| Cond | 声望 ≥ 85 + 均分 ≥ 74 |
| condFn | `() => S.rank >= 2 && S.realN >= 2 && (S.realQ.length ? S.realQ.reduce((a,b) => a+b, 0) / S.realQ.length : 0) >= 74`（`checkEnd()` 已复用此条件，P-09 已修复） |
| 功能兑现 | ⚠️ 现状：自动触发入驻弹窗。设计意图：解锁入驻**资格**，邀请由孵化器人做决策（happy-paths §4 Phase 3）；黄金达成时按 D36 同步写 `S.profile.certs.gold`（已落地） |
| ⚠️ | P-16 已批准：将移出树，进入认证徽章架 |

#### N-12 · 教练认证（教练树根）

| 字段 | 值 |
|---|---|
| Tier | 3.5（角色） |
| Verb | 带教 |
| Icon | 🎓 |
| Group | role |
| Deps | N-07（自动派生） |
| Cond | 联机版：带队 ≥ 5 家 · 单机版待定 |
| condFn | `() => false`（联机版功能，单机版永久锁定） |
| 功能兑现 | ❌ 教练树诞生 + 教练视图面板。单机版替代条件见 GDD v0.14 开放问题 #11 |

#### N-13 · 孵化认证（孵化器树根）

| 字段 | 值 |
|---|---|
| Tier | 3.5（角色） |
| Verb | 孵化 |
| Icon | 🏭 |
| Group | role |
| Deps | N-07（自动派生） |
| Cond | 外部资质（孵化平台授予） |
| condFn | `() => false`（外部授予，游戏内不可自行解锁） |
| 功能兑现 | ❌ **目标 2**：孵化器树诞生 + 孵化器工作台面板，见 §8.2 |

#### N-14 · 平台运营（平台运营树根）

| 字段 | 值 |
|---|---|
| Tier | 3.5（角色） |
| Verb | 治理 |
| Icon | 🦄 |
| Group | ezagent |
| Deps | N-07（自动派生） |
| Cond | ezagent 团队内部资质 |
| condFn | `() => false`（ezagent 团队内部，玩家不可解锁） |
| 功能兑现 | ❌ 平台运营树诞生 + 平台治理面板：平台配置、Connector 生态管理、系统健康监控、目标管理（world.cup） |

### 4.2 个人树（经历线，scope: `'personal'`）

> 个人树不受公司破产影响，节点状态跟随主理人档案跨公司保留。condFn 只读个人字段（§3.3）。
> ⚠️ P-15 已批准、待落地：P-00~P-03 为计数节点，将移出树改为个人页经验条；个人树只留认证/动词类节点。

#### P-00 · 跨组织从业

| 字段 | 值 |
|---|---|
| Tier | 0（根） |
| Verb | 连接 |
| Icon | 🔗 |
| Group | base |
| Deps | — |
| Cond | 完成打工 ≥ 1 次 |
| condFn | `() => S.profile.gigs >= 1`（P-04 已修复：读真实打工计数，不再读人脉） |
| 功能兑现 | ❌ 待定；⚠️ 将移出树改经验条（P-15） |

#### P-01 · 作品积累

| 字段 | 值 |
|---|---|
| Tier | 1 |
| Verb | 归档 |
| Icon | 🗂️ |
| Group | specialty |
| Deps | P-00（自动派生） |
| Cond | 作品集 ≥ 3 |
| condFn | `() => S.profile.portfolio >= 3` |
| 功能兑现 | ❌ 待定；⚠️ 将移出树改经验条（P-15） |

#### P-02 · 人脉经营

| 字段 | 值 |
|---|---|
| Tier | 1 |
| Verb | 拓展 |
| Icon | 🤝 |
| Group | specialty |
| Deps | P-00（自动派生） |
| Cond | 人脉 ≥ 3 |
| condFn | `() => S.profile.net >= 3` |
| 功能兑现 | ❌ 待定；⚠️ 将移出树改经验条（P-15） |

#### P-03 · 复盘沉淀

| 字段 | 值 |
|---|---|
| Tier | 2 |
| Verb | 反思 |
| Icon | 📝 |
| Group | deepen |
| Deps | P-01（自动派生） |
| Cond | 复盘素材 ≥ 5 |
| condFn | `() => S.reviews.length >= 5` |
| 功能兑现 | ❌ 待定；⚠️ 将移出树改经验条（P-15） |

#### P-04 · 判断力认证

| 字段 | 值 |
|---|---|
| Tier | 2.5（资质门） |
| Verb | 认证 |
| Icon | 🎯 |
| Group | gate |
| Deps | P-02（自动派生） |
| Cond | 判断力 ≥ 60%（至少 5 次判断） |
| condFn | `() => {const jt = S.profile.judgeT; return jt >= 5 && S.profile.judgeG / jt >= 0.6;}` |
| 功能兑现 | ❌ 待定；⚠️ 属认证类，P-16 落地时进入个人认证徽章架 |

#### P-05 · 孵化器背书

| 字段 | 值 |
|---|---|
| Tier | 3 |
| Verb | 背书 |
| Icon | 🏭 |
| Group | cross |
| Deps | P-04（自动派生） |
| Cond | 加入孵化器预备营 |
| condFn | `() => S.profile.guildHist.incu === true`（P-05 已修复：读个人档案会籍记录，不读公司会籍状态） |
| 功能兑现 | ❌ 待定 |

#### P-06 · 创业导师

| 字段 | 值 |
|---|---|
| Tier | 3.5（角色） |
| Verb | 带教 |
| Icon | 🎓 |
| Group | role |
| Deps | P-05（自动派生） |
| Cond | 黄金认证（声望 ≥ 85） |
| condFn | `() => S.profile.certs.gold === true`（P-06 已修复：读个人认证记录 D36，不读公司 rank） |
| 功能兑现 | ❌ 待定 |

---

## 5. How —— 实现细节

### 5.1 渲染架构

两棵树共享同一套渲染引擎（`renderRadialTree(scope)` + `nodeAngle(n, scope)`），通过 `scope` 字段区分。资质树的渲染复用同一引擎（新增 scope + 参数行，见 §7.1）。

| 参数 | 公司主干树 | 个人树 |
|---|---|---|
| SVG viewBox | `0 0 720 740` | `0 0 620 500` |
| 中心点 | `(360, 390)` | `(310, 265)` |
| Tier 半径 | `0:0, 1:78, 2:150, 2.5:220, 3:285, 3.5:350` | `0:0, 1:48, 2:95, 2.5:138, 3:180, 3.5:225` |
| 节点圆半径 | tier 0 = 20，其他 = 16 | 统一 = 12 |
| 连线 | 正交路径 + `stroke-linejoin:round` | 同左 |
| 平移容器 | `#treePanWrap` | `#personalTreePanWrap` |

### 5.2 连线逻辑

连线从父节点（`deps[]`）指向子节点，使用正交路径：

```
M parentX parentY  →  L parentX midY  →  L childX midY  →  L childX childY
```

`midY = parentY + (childY - parentY) * 0.55`（偏向父节点一端）。

CSS: `stroke-width:3; stroke-linejoin:round; stroke-linecap:round`。
状态 class：`unlocked`（子节点已解锁 → 实线 teal）、`available`（仅父节点解锁 → 虚线 gold）、默认（虚线 gray）。

### 5.3 节点状态机

每个节点在 `S.treeNodes[id]` 中存储 `{unlocked: boolean}`。

```javascript
// 初始化（$('startBtn').onclick 中调用）
function initTreeNodes(){
  TREE_NODES.forEach(n => {S.treeNodes[n.id] = {unlocked: n.condFn()};});
}

// 可解锁判断（P-01）：父节点检查从 deps 自动派生，condFn 只写额外条件
function nodeAvail(n){
  return n.deps.every(d => {const ds = S.treeNodes[d]; return ds && ds.unlocked;}) && n.condFn();
}

// 解锁检查（tick() 周结算记日志；renderRadialTree() 内静默调用）
function checkTreeNodes(){
  let newly = [];
  TREE_NODES.forEach(n => {
    if (!S.treeNodes[n.id].unlocked && nodeAvail(n)) {
      S.treeNodes[n.id].unlocked = true;
      newly.push(n);
    }
  });
  return newly;
}
```

### 5.4 三种渲染状态

| 状态 | CSS class | 条件 | 视觉效果 |
|---|---|---|---|
| unlocked | `.tnode.unlocked` | `S.treeNodes[id].unlocked === true` | cream 填充 + teal 描边 |
| available | `.tnode.available` | `!unlocked && nodeAvail(n)` | 金色虚线描边 + 脉冲动画 |
| locked | `.tnode.locked` | 以上皆否 | 灰色填充 + 灰色描边 · `cursor:default` |

连线对应三种状态：`.tline.unlocked`（teal）、`.tline.available`（金色虚线）、默认（灰色虚线）。

### 5.5 渲染管线

```
renderRadialTree(scope)
  ├── checkTreeNodes()                  // 刷新解锁状态（静默）
  ├── filter TREE_NODES by scope        // 选出当前树的节点
  ├── 背景环 [1, 2, 2.5, 3, 3.5]       // <circle class="tree-ring">
  ├── 连线 (n.deps → parent)            // <path> 正交路径
  │   └── nodeAngle(n, scope) → 角度   // 按 scope 过滤同 tier 节点后均分（π*0.12 → π*0.88）
  └── 节点                              // <circle class="tnode"> + <text>
      └── onclick → openNodeDetail(id)  // 弹窗：图标/名称/verb/等级条/条件/依赖
```

### 5.6 关键函数签名

```javascript
function renderRadialTree(scope)        // scope = 'company' | 'personal'，默认 'company'
function nodeAngle(n, scope)            // 返回节点在环上的弧度角
function nodeAvail(n)                   // deps 全部解锁 && n.condFn()（P-01）
function initTreePan(wrapId)            // 鼠标/触摸拖拽平移，wrapId 默认 'treePanWrap'
function openNodeDetail(nid)            // 节点详情弹窗（搜索全部 TREE_NODES，不限 scope）
```

### 5.7 数据源映射

主干树的 `condFn` 读取以下 `S` 字段：

| S 字段 | 类型 | 被哪些节点使用 |
|---|---|---|
| `S.started` | bool | N-00 |
| `S.credits.design` / `.copy` / `.growth` | number | N-01 / N-02 / N-03 |
| `S.lines.design` / `.copy` / `.growth` | number | N-04 / N-05 / N-06 |
| `S.rep` | number | N-07, N-11 |
| `S.cred` | number | N-07 |
| `S.treeNodes['N-01'..'N-06']` | object | N-08（≥2 条 T1）, N-09（任一 T2）——父节点检查由 nodeAvail 派生 |
| `S.profile.portfolio` | number | N-10 |
| `S.rank` | number | N-11 |
| `S.realN` / `S.realQ` | number / array | N-11 |

个人树的 `condFn` 读取以下字段（全部个人字段，§3.3 约束已落实）：

| S 字段 | 类型 | 被哪些节点使用 |
|---|---|---|
| `S.profile.gigs` | number | P-00 |
| `S.profile.portfolio` | number | P-01 |
| `S.profile.net` | number | P-02 |
| `S.reviews.length` | number | P-03 |
| `S.profile.judgeT` / `.judgeG` | number | P-04 |
| `S.profile.guildHist.incu` | bool | P-05 |
| `S.profile.certs.gold` | bool | P-06 |

字段增长来源（实现事实）：

| 字段 | 增长时机 | 代码位置 |
|---|---|---|
| `S.credits[dm]` | 练习单交付**且班内竞赛排名前 50%（4 队前 2 名）**，按环节每个 +1（D40） | `:1282` |
| `S.lines[dm]` | 真单交付，按执行环节（stages[1] 或 [0]）+1 | `:1293-1294` |
| `S.profile.portfolio` | 真单交付 +1 | `:1292` |
| `S.profile.net` | 打工 +1；商务社交 +1 | `:1059`, `:1409` |
| `S.profile.gigs` | 打工完成 +1（P-04） | `:1059` |
| `S.profile.guildHist[k]` | 加入会籍时置 true（P-05，个人档案永久记录） | `:1659` |
| `S.profile.certs.gold` | 公司达成黄金认证时同步置 true（P-06 / D36） | `:1368` |
| `S.profile.judgeT/G` | 环节完成时判断；跑赢基准则 G+1 | `:1345` |

---

## 6. 问题登记与修订

> Living 核心。问题按发现顺序编号，状态：`待决策` / `已决策` / `已修复` / `已批准`。修复后勾掉并写入 §9。

| # | 问题 | 现状（代码事实） | 影响 | 决定 | 状态 |
|---|---|---|---|---|---|
| P-01 | deps 与 condFn 双重事实源 | ~~`deps` 只画线，`condFn` 各自重新检查父节点~~ | 改一处不改另一处 → 逻辑/视觉分裂 | **已修复 2026-07-27**：新增 `nodeAvail(n)`，父节点检查从 `deps` 自动派生；N-04~N-10 的 condFn 已简化为只写额外条件 | ✅ 已修复 |
| P-02 | N-07 资质门是孤点 | `deps:[]`，无连线指向 | 视觉悬浮 | ruihua 判断：白银/黄金认证本就不是技能树的一部分，而是独立的认证体系——被 P-16 覆盖，**已随 P-16 获 Allen 批准**，认证移出树后本条消解 | ✅ 已批准（随 P-16 落地） |
| P-03 | 学分来源与 D32 不符 | ~~`S.credits` 练习单交付即 +1，无质量门~~ | 计数≠水平 | **已修复 2026-07-27（D40）**：练习单 = 课程作业代理，班内竞赛前 50% 才计学分（`:1282`）；N-01/02/03 条件文案已改 | ✅ 已修复 |
| P-04 | P-00 条件语义错误 | ~~condFn 判 `S.profile.net >= 1`，人脉可刷~~ | 没打过工也解锁「跨组织从业」 | **已修复 2026-07-27**：新增 `S.profile.gigs` 计数（`:1059`），condFn 改判 `gigs >= 1` | ✅ 已修复 |
| P-05 | P-05 读公司状态 | ~~condFn = `S.guilds.incu`~~ | 与个人树「跨公司保留」矛盾 | **已修复 2026-07-27（D36 机制）**：加入会籍时写 `S.profile.guildHist[k]=true`（`:1659`），condFn 改判 `guildHist.incu` | ✅ 已修复 |
| P-06 | P-06 读公司状态 | ~~condFn = `S.rank >= 2`~~ | 同上 | **已修复 2026-07-27（D36）**：公司达成黄金时写 `S.profile.certs.gold=true`（`:1368`），condFn 改判它 | ✅ 已修复 |
| P-07 | strategy/comm 学分是死数据 | 有积累、无节点消费 | 档案页 5 项学分 2 项无意义 | **已修复 2026-07-27（ruihua 方案）**：有节点的三类（设计/文案/增长）置顶显示；策略/沟通收进「未开启」折叠面板（`<details>`），数据继续积累、开线后可用 | ✅ 已修复 |
| P-08 | **节点不兑现功能（D31 失败）** | N-01~N-10 解锁只出一行日志 | 按动词规则，T2 以上全是失败节点 | **已决策**：建立 `grants` 通用机制（§3.4），孵化器树是第一个完整用例 | 已决策 |
| P-09 | N-11 与 checkEnd() 条件重复 | ~~同一套条件写两遍~~ | 改门槛时漏改一处 | **已修复 2026-07-27**：`checkEnd()` 改为复用 `TREE_NODES['N-11'].condFn()` | ✅ 已修复 |
| P-10 | **专精激活线未实现** | GDD §5.10.4：同一时间仅一条 T1 线享 +30% 训练效率，切换冷却 2 周 | demo 完全没有该概念 | 提议：demo 阶段标注「暂不实现」，或加最小版（激活线选择 + 日志提示） | 待决策 |
| P-11 | **locked 节点显示名称** | GDD §5.10.6：锁定节点显示条件但不显示名称；demo 直接显示 | Explorer 的发现感被剧透 | 提议：locked 节点名显示为「？？？」，available 后才显示 | 待决策 |
| P-12 | **个人树节点无任何兑现** | P-00~P-06 全是装饰 | 个人树没有存在意义 | 方向：P-00~P-03 移出树改经验条（P-15 已批准）；剩余节点的动词单独设计一轮；grants 机制共用 | 待决策 |
| P-13 | **解锁通知太弱** | 仅日志一行（flywheel-audit C.6-1） | Content progression 无感知 | 候选插队项：全屏 banner + 教练消息 | 待决策 |
| P-14 | 死代码 `RING_RADII` | ~~`:1668` 定义未使用~~ | 误导读者 | **已修复 2026-07-27**：已删除，全文无残留 | ✅ 已修复 |
| P-15 | **技能/经验两套度量混淆（框架级）** | 现状：计数（练习单数/打工数/人脉数）直接解锁树节点 | 是 P-03/P-07 等的上游，决定现有 T1 节点条件合法性 | **✅ 已批准（Allen，2026-07-27，PR #1543）**：技能 = 水平，仅由质量信号（课程学分/资格认证/竞赛与专家评级/外部认证录入）衡量 → 解锁技能树；经验 = 交付计数（订单/打工/作品/人脉）→ 进度条展示（平台活跃度），派生信用与声望，不解锁树；公司页与个人页均为「上经验条 + 下技能树」。A/B 案例见 §2.2。落地清单见 §8.1；D37 随之废止 | ✅ 已批准（待落地） |
| P-16 | **认证与技能树是两套体系** | N-07/N-11 作为「资质门」挂在树上（v0.14 设计），但条件（声望/信用）属声誉体系，与树的「学分/动词」逻辑不同源 | 是 P-02（N-07 孤点）的根因 | **✅ 已批准（Allen，2026-07-27）**：青铜/白银/黄金 = 协会基于声望/信用颁发的认证阶梯，独立于技能树；认证作为**输入**解锁树节点与资质树，本身不挂树；技能树只剩纯动词节点；认证单独做「徽章架」展示。个人侧同理（判断力认证等）。落地：N-07/N-11 移出 TREE_NODES，N-08/09/10 重锚到专精线，N-12/13/14 保留「同节点兼作资质树根」语义 | ✅ 已批准（待落地） |

---

## 7. SOP

> **v0.15 修订**：本节现有“节点数组 / 资质树根 / grants”步骤只适用于 v0.14 代码维护。新增成长树节点时必须先按 `platform-concept-model.md` 判断它是不是可评价的能力；认证、资质、会籍和平台功能不得按本节旧流程加入成长树。

### 7.1 技术 SOP —— 如何加节点 / tier / 树

#### 新增一个主干树节点

在 `TREE_NODES` 数组中，`N-14` 之后、`// ---- 个人成长树节点 ----` 注释之前插入：

```javascript
{id:'N-15',tier:4,name:'新节点名称',verb:'动作',icon:'🆕',group:'newgroup',scope:'company',deps:['N-11'],
 cond:'条件描述',condFn:()=>/* 只写额外条件 */,level:{cur:0,max:1}},
```

检查清单：

1. `verb` 在整棵树上只出现一次（D31 动词规则）
2. 只写 `deps` + 额外条件——父节点检查由 `nodeAvail` 自动派生（P-01 已修复），不要在 `condFn` 里手写父节点检查
3. 声明 `grants`（§3.4）；没有兑现的节点不允许合入
4. 在本文 §4 补节点定义表
5. §9 变更记录 +1

#### 新增一个个人树节点

在 `P-06` 之后、`];` 之前插入（格式同上，`scope:'personal'`，`deps` 引用 P-xx 节点）。
额外约束：`condFn` 只读个人字段（§3.3）；P-15 批准后：计数类条件一律进经验条，不做树节点。

#### 新增一棵资质树（如孵化器树）

1. 主干树上的认证节点（根）已存在（如 N-13）；其 `condFn` 通常为外部授予
2. 子节点用新 scope（如 `'incubator'`）加入节点数组，deps 引用根节点 id；渲染时根节点同时出现在主干树（`scope:'company'`）和资质树（`scope:'incubator'`）的过滤器中（同节点兼作树根，D35）
3. `renderRadialTree` 参数表加一行（viewBox/中心/半径），背景环数组按 tier 调整
4. 资质树的访问路径 = 对应功能面板内嵌（不是 ERP tab）
5. 声明每个子节点的 `grants` 并接到 UI 权限查询

#### 新增 tier

1. 在 `renderRadialTree` 中扩展 `radii` 映射（每个 scope 都需要对应半径）
2. 在背景环渲染的数组中添加新 tier
3. 确保新 tier 的半径不超过 viewBox 范围

### 7.2 运营 SOP —— 谁能开新树 / 新分支

> 基础决策：**D38 —— 树内容由平台统一定义**（2026-07-27）。

**原则：树的内容是平台级公共品，定义权集中在平台；角色组织获得的是解锁状态，不是定义权。**

| 角色 | 能做什么 | 不能做什么 |
|---|---|---|
| **平台运营**（ezagent，N-14）= 现实中的游戏策划 | 新增/修改任何树的节点、分支、tier；调整解锁数值；定义新资质树 | — |
| **协会** | 提案认证门（gate）节点的门槛数值（认证标准本就是协会资产）；提案新认证节点 | 直接上线——仍需平台运营合入 |
| **孵化器/其他角色组织** | 解锁资质树后获得面板使用权；可提案自己业务需要的功能子节点 | 自定义树结构 |
| **普通组织（OPC）** | 解锁节点；看到全树 | 提案需经协会或平台渠道 |

**新节点上线流程**：

1. 提案：写明 verb、解锁条件、功能兑现（grants）、数值依据
2. 评审：动词唯一性检查 + 数值 playtest
3. 合入：按 §7.1 技术 SOP 实施
4. 公开：树结构变更对所有用户可见（D28），在结算日事件中公告
5. 记录：本文 §6（如有问题修订）+ §9 变更记录

**开「新资质树」的判定**：默认作为现有主干树认证节点的展开（D35）。只有当面板/视角/字段完全不同于现有类型时，才考虑全新树型，且需本文档重大版本记录。

**待决策**：

- 组织自定义「目标节点」（GDD 开放问题 #9 的世界杯模式）是否开放？当前建议：demo 阶段不开放，待目标树讨论有结论
- 数值门槛的最终拍板权：平台运营单方决定，还是需要协会联署？（认证通胀风险，gap-analysis #17）
- 外部认证录入（D41）的核验机制：协会认证 Agent 初审 + 人工抽查？核验失败的惩罚？

---

## 8. 路线图

### 8.1 目标 1：v0.15 概念分层与成长树（2026-07-28）

1. ~~P-01（双重事实源）~~ ✅、~~P-03（D40 质量门）~~ ✅、~~P-04/P-05/P-06（个人树去污染）~~ ✅、~~P-07（显示分层）~~ ✅、~~P-09（重复条件）~~ ✅、~~P-14（死代码）~~ ✅ —— 2026-07-27 修复包已落地
2. **v0.15 落地包**：
   - **概念模型**：以 `platform-concept-model.md` 统一经验、能力、信誉等级、专业认证、角色资质、背书、会籍与平台功能
   - **旧节点迁移**：N-07/N-11、P-04 移入徽章架；N-08/N-09/N-10 移入功能开放面板；P-00~P-03 移入经验履历
   - **个人成长树**：六项 OPC 核心能力 + 示例专业分支；节点只表示可评价的达标结果
   - **公司成长树**：展示稳定交付、协作、方法沉淀等组织能力，不等于成员/Agent 能力并集
   - **外部认证录入**：档案页最小表单，录入后标注“平台外 / 待核验”
   - **候选档案**：展示能力、经验、认证、证据、缺口与下一道验证任务，不设 OPC 总分
   - D35–D47 写入 GDD v0.15
3. v0.14 `TREE_NODES` 细节在 §3~§5 保留为迁移台账，完成 v0.15 demo 后再删除已无实现用途的旧条目。

### 8.2 目标 2：孵化器角色成长树 + 孵化器工作台

**结构修订（D47）**：孵化平台资质不作为树节点。组织取得资质并由平台开通工作台后，孵化器角色成长树开始记录履职能力。

```
孵化平台角色资质 + 平台开通 → 孵化器工作台
  ├─ 候选项目判断达标
  ├─ 支持方案设计达标
  └─ 投后管理达标

工作台功能（独立开放规则）：
  Scouting · 定向命题 · grant/桥贷 · 投后管理
```

角色能力的达标条件必须包含履职质量评价；发布命题数、追踪企业数和 grant 次数只算经验，不能单独使能力达标。

**demo 实现路径**：单机 demo 中没有真孵化器玩家——用「角色扮演入口」模拟：玩家以 DHS 孵化器身份查看同一张基础面板 + 孵化器树解锁状态（复用已有 `data-view` 高亮机制的思路）。

需求来源：[`v0.13/happy-paths-v0.13.md`](../versions/v0.13/docs/happy-paths-v0.13.md) §4（孵化器 Happy Path 三阶段）。

---

## 9. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-07-24 | v0.14 | 初始版本：15 个公司节点 + 7 个个人节点，scope 分离，正交连线，living doc 创建 |
| 2026-07-27 | — | 迁移至 `doc/tree/skill-tree.md`，扩为全量 living doc：新增文档地图、设计原则、问题登记（P-01~P-14）、运营 SOP 草案、路线图；修正渲染参数与代码的漂移 |
| 2026-07-27 | — | 按 what→why→how 重构全文；订立 D35（多树结构，修订 D33）、D36（认证双重记录）、D37（打工仅解锁个人树，D32 精确化）、D38（树内容平台统一定义）；新增 §3 完整逻辑（诞生/解锁/兑现/消亡）；P-04/P-05/P-06/P-08 标记已决策；资质树扩展 SOP 落地 |
| 2026-07-27 | — | 订立 D39（成长归属=资产归属，对开放问题 #12 倾向「否」）、D40（学分代理：练习单+质量门模拟教务学分）；新增 §3.6 成长归属；登记 P-15（技能/经验框架提案，待 Allen 批准）；P-03 标记已决策 |
| 2026-07-27 | — | demo 修复包落地（`MFU-v0.14-可试玩原型.html`，node --check 通过）：P-01（`nodeAvail` 依赖自动派生 + N-04~N-10 condFn 简化）、P-03（D40 质量门 `:1282` + N-01/02/03 文案）、P-04（`S.profile.gigs`）、P-05（`guildHist.incu`）、P-06（`certs.gold` 双重记录）、P-07（学分显示分层 + 「未开启」折叠面板）、P-09（`checkEnd` 复用 N-11 条件）、P-14（删 `RING_RADII`）；登记 P-16（认证独立于技能树，ruihua 方向），P-02 被 P-16 覆盖 |
| 2026-07-27 | — | **P-15/P-16 获 Allen 批准**（PR #1543 merge）：技能/经验框架 + 认证独立体系成为正式方向；订立 D41（外部认证录入，B 案例）；D37 被框架取代（打工只进经验条）；§2.2 补三体系全景图与 A/B 市场价值案例；工作移至新分支 `mfu-demo-tree`，进入 v0.15 落地阶段（§8.1） |
| 2026-07-28 | v0.15 | 建立 `platform-concept-model.md`；订立 D42–D47：成长树与平台功能分离、节点只表达达标结果、宽领域与放射分支、多校课程映射、学生/Agent 贡献归属、个人/公司/角色三类成长树；加入 v0.14 全节点迁移表 |

---

## 附录 A. 文档地图 —— 与已有文档的关系

| 文档 | 性质 | 与本文档的关系 |
|---|---|---|
| GDD v0.13 [`v0.13/MFU-策划案-GDD-v0.13.md`](../versions/v0.13/docs/MFU-策划案-GDD-v0.13.md) D28 | 快照 | 三棵成长树的原始决策，已被 D33→D35 演化取代 |
| GDD v0.14 [`v0.14/MFU-策划案-GDD-v0.14.md`](../versions/v0.14/docs/MFU-策划案-GDD-v0.14.md) §5.10 | 快照 | 科技树系统设计案（D31–D33、动词规则、GDKeys 落地）。本文 §2 是其浓缩 + 持续迭代 |
| [`v0.13/flywheel-audit-v0.13.md`](../versions/v0.13/docs/flywheel-audit-v0.13.md) §5 | 快照 | 「科技树作为统一 progression 与权限体系」的设计回应，§2.1 问题清单的来源 |
| [`v0.13/happy-paths-v0.13.md`](../versions/v0.13/docs/happy-paths-v0.13.md) §4 | 快照 | 孵化器 happy path，孵化器树（§8.2）的需求来源 |
| [`v0.13/gap-analysis-v0.13.md`](../versions/v0.13/docs/gap-analysis-v0.13.md) | 快照 | 仿真差距清单，与本树无直接耦合 |
| [`../versions/v0.14/demo/MFU-v0.14-可试玩原型.html`](../versions/v0.14/demo/MFU-v0.14-可试玩原型.html) | 历史实现 | v0.14 成长树快照；当前平台概念以 `platform-concept-model.md` 与最新 Demo 为准 |
