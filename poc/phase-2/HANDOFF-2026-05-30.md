# Migration Handoff — 2026-05-30 (pre-compact, end-of-day)

> Written at 91% context before a compact, switching machines. Resume-reader:
> read this FIRST. It supersedes `docs/ezagent-migration/HANDOFF-2026-05-28.md`
> (in the AutoService repo) for everything after the Phase-2 PoC.

## One-paragraph status

AutoService → ezagent migration. **Two phases are DONE and verified end-to-end
in a browser**: (1) the customer-chat **frontend** (LiveView `/chat/:tenant` +
embeddable widget + operator console), and (2) **extracting all of it into a
dedicated `ezagent_plugin_customer_chat` plugin** + a direct `/operator/:tenant`
operator entry + a real operator cap. Both are pushed to **draft PR
ezagent42/ezagent#446** (`poc/phase-2-customer-service`). **The current task is a
brainstorm for the THIRD core capability — "admin iterates the AI" (edit the
soul / 话术 / KB / service flow).** We scoped this round to the smallest slice
(an editable soul) and just **presented a design that is awaiting the user's
approval** (see §Current Task). Next action after approval: write the spec, then
writing-plans, then subagent-driven implementation.

## Where everything is

- **ezagent repo**: `~/workspace/ezagent42/ezagent` (main). The actual platform.
- **Active worktree**: `~/workspace/ezagent42/ezagent-poc-phase-2`, branch
  `poc/phase-2-customer-service`. **ALL the work is here, all pushed.** At home:
  `git clone git@github.com:ezagent42/ezagent.git && cd ezagent && git fetch &&
  git checkout poc/phase-2-customer-service` (or add a worktree). HEAD at handoff:
  `f27f0788`.
- **AutoService repo**: `~/workspace/ezagent42/AutoService` — only *migration
  design docs* live here (`docs/ezagent-migration/`). This is a separate repo;
  the editor's "click a relative path" only resolves under whichever repo you
  opened, which is why `poc/...` paths don't open from an AutoService window.
  **All PoC docs are in the EZAGENT repo at `poc/phase-2/`** — open that folder.
- **Shared deps cache**: `~/workspace/ezagent42/.poc-shared-deps`. Every mix
  command MUST be prefixed `MIX_DEPS_PATH=/Users/daiming/workspace/ezagent42/.poc-shared-deps`.
  **NEVER run `mix deps.get`** (corrupts the shared cache). NOTE: this absolute
  path is machine-specific — at home, re-create the shared deps dir (or point
  MIX_DEPS_PATH at a fresh `mix deps.get` location) and re-create the
  `deps` symlink (see Asset gotcha).
- **PR**: https://github.com/ezagent42/ezagent/pull/446 — DRAFT/WIP (held for
  review on purpose). AutoService PR #297 (design docs) is untouched/separate.

## Running server (may be down after you leave)

- Port **10142**, profile **poc-phase2**, node `ezagent_runtime_phase2@127.0.0.1`.
- Restart (and the asset gotcha) — see `poc/phase-2/10-customer-chat-plugin-extraction-MANUAL-TEST.md` appendix. Short version:
  ```
  cd ~/workspace/ezagent42/ezagent-poc-phase-2
  [ -e deps ] || ln -s /Users/daiming/workspace/ezagent42/.poc-shared-deps deps   # NODE_PATH for esbuild
  EZAGENT_PROFILE=poc-phase2 PORT=10142 \
    EZAGENT_BRIDGE_WS_URL=ws://127.0.0.1:10142/agent_bridge/websocket \
    EZAGENT_RUNTIME_NODE=ezagent_runtime_phase2@127.0.0.1 \
    MIX_DEPS_PATH=/Users/daiming/workspace/ezagent42/.poc-shared-deps \
    env -u USE_LOCAL_OAUTH -u ANTHROPIC_API_KEY -u CLAUDE_CODE_DISABLE_CRON -u USE_STAGING_OAUTH \
    mix phx.server
  # if the page has no styling / layout broken:
  MIX_DEPS_PATH=.../.poc-shared-deps mix esbuild ezagent_web && MIX_DEPS_PATH=.../.poc-shared-deps mix tailwind ezagent_web
  ```
- Login: `entity://user/system/admin` / `ezagent-dev`. Tenant: `acme`.

## What's DONE (and verified in a real browser)

### Phase 3 — customer-chat frontend (commits up to ~`9a8a81cb`)
Public LiveView `/chat/:tenant` (themed, live AI reply, reload-resume via
localStorage), embeddable `widget.js` (iframe of `?embed=1`), operator console.
3 e2e bugs fixed (assets 404 / widget CSRF / iframe CSP). Spec+plan:
`8-customer-chat-frontend-design.md` / `-PLAN.md`. Acceptance in `ACCEPTANCE.md`.

### Plugin extraction (commits `cb19f314`..`f27f0788`)
`ezagent_plugin_customer_chat` is now a real plugin:
- Shows on `/plugins` ("Customer Service" card → `/operator` via `config_surface`).
- Customer ChatLive + widget + operator console (`DashboardLive`/`SessionViewLive`)
  + `Bootstrap`/`Theme`/`Components`/`OperatorAuth` all live in the new app.
- `Behavior.Mode` stayed in `ezagent_domain_chat` (generic). `/sessions` untouched.
- **`/operator/:tenant` direct entry** — tenant baked into the route, NO workspace
  switch (the friction it set out to kill). `/operator/:tenant/:conv` = session detail.
- **Operator cap** = workspace-scoped `Mode.set` (admin passes via system-member),
  in `OperatorAuth` — replaced the PoC "any cap" gate.
- Legacy `/admin/customer_sessions` redirect was REMOVED (user call: PoC, no users
  know the old URL).
- Spec+plan: `10-customer-chat-plugin-extraction-design.md` / `-PLAN.md`.
  Acceptance + the 4 bugs the gates caught: bottom of `ACCEPTANCE.md`.
- **User manually tested all 7 cases — ALL OK** (`10-...-MANUAL-TEST.md`).
- 13 unit tests green; our code `--warnings-as-errors` clean; plugin registers in
  `PluginRegistry`.

## CURRENT TASK (resume here): admin-edit brainstorm — DESIGN AWAITING APPROVAL

We are in `superpowers:brainstorming` for the 3rd capability: **admin iterates
the AI**. Settled decisions:

**Strategic (the big one — locked in discussion):**
- Do NOT cargo-cult AutoService's soul-layer decomposition (L0/L1/L2/L3 + flow_directives
  + KB chunks + slot templates + CR + lint). That complexity was *earned* by
  AutoService's production scale (cinnox 80KB soul, multi-tenant, governance) — it is
  the end-state of iterating on real pain, not a starting requirement.
- **Approach = incremental + empirical A/B.** Build the smallest slice, then test
  with **cinnox** (NOT acme — acme's soul is too tiny to reveal any pain) using a
  small fixed case set + simple rubric (right facts / follows flow / no internal-leak /
  no drift), same model. Each *next* scope (KB? flow? layers?) is gated by a measured
  A/B (with vs without) on cinnox cases; **if it doesn't measurably improve answers,
  drop it.** AutoService's pieces = a catalog of proven solutions to specific pains;
  adopt each only when the pain shows + re-abstract onto ezagent primitives (fewer
  modules — conceptually just 3: compose / retrieve-selectively / edit-safely).
- **Sequencing decided:** (1) build scope #1 = editable soul (FUNCTIONAL only —
  "edit → new conversation uses it"; the quality A/B starts at scope #2). (2) THEN a
  separate cycle to build the cinnox A/B measurement harness. (3) Use the data to pick
  scope #2.

**Scope #1 (this brainstorm) — editable soul. DESIGN PRESENTED, AWAITING APPROVAL:**
1. **Scope**: admin edits a tenant's customer soul (whole markdown), save → new
   conversations auto-use it. No layers/KB/flow/CR/lint/versioning. Tenant-parameterized.
2. **Storage (DECIDED: sandbox + fixture seed)**: edits write to
   `<sandbox_root>/<tenant>/souls/<role>.md`; `Bootstrap.cc_soul_path_for_workspace/2`
   reads sandbox-first, falls back to the repo fixture (seed). `sandbox_root` reuses
   the existing `:customer_chat_sandbox_root` config (currently `~/poc-sandbox-phase2`).
   cc_agent already `File.read`s that path + prepends `channel_preamble()`, so edited
   souls keep the protocol preamble automatically; the editor edits only the soul body.
3. **Edit UI**: new `EzagentPluginCustomerChat.SoulEditorLive`, route
   `/operator/:tenant/soul`, reached via a link on the operator dashboard. Loads the
   effective soul (sandbox else fixture) into a textarea; Save writes sandbox; "Reset
   to default" deletes the sandbox file (falls back to fixture). Gated by the operator
   cap (admin passes); production should split a distinct "configure" cap (noted).
4. **Take-effect**: new conversations read the sandbox soul at spawn (demo: edit →
   open a NEW conversation → see the change). Live per-conv agents keep their soul (no
   forced recycle in PoC; `recycle_tenant`-style is a nice-to-have, deferred).
5. **Generic vs specific**: editor + sandbox storage = CS-shaped → customer-chat
   plugin. cc soul_path read mechanism = existing generic cc primitive, unchanged.
6. **Testing**: unit (soul-store sandbox-vs-fixture resolution; save/reset);
   functional e2e (edit acme soul → new conversation reflects it; reset → back to fixture).
7. **NOT doing**: layers, KB, flow, CR, lint, versioning/review, forced recycle, slot
   templates (all deferred to the A/B roadmap).

**Open sub-decisions I asked the user (NOT yet answered — ask again on resume):**
route name `/operator/:tenant/soul`? keep "Reset to default"? split a configure cap
this round or reuse operator cap? add a "recycle live agents now" button? — The user
pivoted to handoff before answering, so re-confirm these (defaults above are fine to
proceed if they say "looks good").

## Exact next steps (in order)

1. Re-present / confirm the scope-#1 editable-soul design above; get approval (and
   the 4 open sub-decisions).
2. Write the spec to `poc/phase-2/11-admin-edit-soul-design.md` (brainstorming step),
   self-review, user review.
3. `superpowers:writing-plans` → `poc/phase-2/11-admin-edit-soul-PLAN.md`.
4. `superpowers:subagent-driven-development` to implement (fresh subagent per task,
   spec + code-quality review on substantive tasks — same cadence as the extraction).
5. Functional e2e in browser (edit acme soul → new conv reflects it).
6. Then the SEPARATE cinnox A/B measurement-harness cycle.

## Non-obvious learnings (do NOT re-derive)

- **Asset/CSS gotcha (bit us twice)**: under shared `MIX_DEPS_PATH`, esbuild's
  `NODE_PATH` points at repo-local `deps/` which doesn't exist → make a `deps` symlink
  to the shared cache, then `mix esbuild`/`mix tailwind`. AND Tailwind v4 `@source`
  globs in `apps/ezagent_web/assets/css/app.css` must list each plugin's lib path —
  the customer-chat plugin's path is now there; any NEW plugin with its own classes
  needs an `@source` line or its CSS gets purged (broke layout silently; compile+tests
  were green — only the live browser caught it).
- **Operator cap**: `Ezagent.Capability` field is `workspace_uri` (NOT `workspace`);
  wrong key returns nil → gate silently denies everyone. `:require_entity` on_mount
  sets `is_system_member?` (admin bypass works).
- **Soul-path hop count**: files at `apps/<app>/lib/<app>/x.ex` are 5 `..` from repo
  root (the old `customer_chat/` subdir was 6). Bootstrap uses 5.
- **Phoenix scope alias**: can't put a route for `EzagentPluginCustomerChat.X` inside
  `scope "/", EzagentPluginLiveview` — the scope prepends its alias. Use a scope with
  the right alias.
- **`deps` symlink + `.DS_Store`** are gitignored now (`/deps`, `.DS_Store`); don't
  re-track them.
- **cc first-send race**: the very first send right after a fresh `/chat` mount can
  occasionally land in the `:connecting` window and drop; retry works. Known, deferred.
- The customer LiveView's persistent connection structurally fixes the old C3 SSE
  120s-timeout tension (operator messages reach the customer live).

## Artifact index (all in EZAGENT repo `poc/phase-2/`)
- `HANDOFF-2026-05-30.md` (this file — read first)
- `8-customer-chat-frontend-{design,PLAN}.md` + `9-operator-surface-vs-generic-console.md`
- `10-customer-chat-plugin-extraction-{design,PLAN,MANUAL-TEST}.md`
- `ACCEPTANCE.md` (frontend + extraction acceptance + bugs caught)
- (next) `11-admin-edit-soul-{design,PLAN}.md`
- AutoService repo: `docs/ezagent-migration/` (verdict, constraints, prior handoff)
