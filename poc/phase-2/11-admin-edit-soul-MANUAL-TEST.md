# Scope #1 可编辑 Soul —— 手动测试计划（含记录表）

> 配套:`11-admin-edit-soul-design.md`(设计)/ `-PLAN.md`(实现计划)。
> 在浏览器里逐步操作,把每步的结果填到「记录」里(✅ 通过 / ❌ 失败 + 备注)。

## 环境 & 登录

- **服务器:** http://127.0.0.1:10142 (已启动)
- **登录:** 用户名 `admin` ,密码 `ezagent-dev`
- **租户(tenant):** `cinnox` —— 编辑器里的「default」soul 就是真实的 89KB cinnox 话术(fixture)

## ⚠️ 重要前提(先读)

- **A1–C3(编辑器本身 + soul 的增删改回退)在本机完全可用**,可以正常在浏览器里测。
- **D 部分「新会话是否真的用上了编辑后的 soul」** —— ✅ **本机现在能拿到真实 AI 回复**(2026-05-30 修了 cc agent 的首次运行对话框卡死问题,见 `CLAUDE.local.md`)。注意两点冷启动注意事项:**(1)** 首次打开 `/chat/cinnox` 冷启 agent 可能 >15s,会先显示一次 "Could not reach the assistant"——**刷新一次页面**(agent 已热)即变 ready;**(2)** 同一 workspace 别同时开多个会话(共享 .mcp.json 会互相踩 token,只有最后一个能绑定)。单会话刷新后即可正常对话。

---

## Part A —— 访问与权限门槛

### A1. 登录
- **操作:** 打开 http://127.0.0.1:10142/operator/cinnox ;若跳到 `/login`,输入 `admin` / `ezagent-dev` → 点「登录」。
- **预期:** 进入 cinnox 的客服后台(顶部显示 "Customer Service · cinnox")。
- **记录:** [x ] 通过  [ ] 失败  备注:______为什么路由有“operator”这个单词，感觉不符合我们的设计风格。________

### A2. 受权限保护的「Configure soul」入口
- **操作:** 看后台页面顶部。
- **预期:** 顶部有一个蓝色 **"Configure soul"** 链接(因为 admin 通过了 workspace-admin 能力校验)。
- **记录:** [ x] 通过  [ ] 失败  备注:_____________这个页面的 “Customer Sessions” 字体颜色太接近背景，很难看出来，换个颜色___

### A3. (快速门槛检查)未登录无法访问
- **操作:** 用一个无痕/隐身窗口(未登录),直接打开
  `http://127.0.0.1:10142/plugins/customer-chat/cinnox/config`。
- **预期:** 被重定向到 `/login`(看不到编辑器)。
- **记录:** [x ] 通过  [ ] 失败  备注:________________
- *(可选,需要的话告诉我:我可以建一个「只有 operator 权限、不是 workspace-admin」的用户,用来验证非管理员也会被挡。这一条已有单元测试覆盖,所以是可选项。)*

---

## Part B —— 编辑器与保存

### B1. 打开编辑器
- **操作:** 点顶部 **"Configure soul"**(或直接访问 `/plugins/customer-chat/cinnox/config`)。
- **预期:** 标题 "Configure Customer Soul · cinnox";徽章显示 **"default"**;文本框里加载了 cinnox 的 soul;**"Revert to previous" 按钮是禁用(灰色)的**。
- **记录:** [ x] 通过  [ ] 失败  备注:________________

### B2. 编辑 + 保存
- **操作:** 点进文本框,跳到最顶部(Cmd+↑),输入一行 `MANUALTEST-MARKER-1` 再回车。点 **Save**。
- **预期:** 出现「已保存」提示;徽章从 "default" 变成 **"customized"**。(Revert 仍然禁用 —— 这是第一次保存。)
- **记录:** [ ] 通过  [x ] 失败  备注:____________点 **Save** 没出现 「已保存」提示___

---

## Part C —— 回退与重置(rollback)

### C1. 第二次保存 → Revert 变可用
- **操作:** 再次在最顶部输入 `MANUALTEST-MARKER-2` + 回车,点 **Save**。
- **预期:** 徽章 "customized";**"Revert to previous" 现在变成可点击**。
- **记录:** [x ] 通过  [ ] 失败  备注:________________

### C2. 回退到上一版
- **操作:** 点 **Revert to previous**。
- **预期:** 文本框回到 **MARKER-1** 那一版(MARKER-2 不见了,MARKER-1 还在)。
- **记录:** [x ] 通过  [ x] 失败  备注:______但是我不确定这和我想象中的回道之前版本的业务设计是否一i样，我不是 为了只在编辑框里回退。我想要的，在应用这个soul之后，可以退回到上一版本。主要原因是，admin修改之后，肯定是要去客服对话里去测试的，测试感觉不好，所以有了回退版本的需求。加入你是产品经理，你觉得我这个设计合理吗？还是不合理，需要修改。________

### C3. 重置为默认(内联确认)
- **操作:** 点 **Reset to default** → 会出现**内联**的 "Reset to default? · Confirm reset · Cancel"(不是浏览器弹窗)→ 点 **Confirm reset**。
- **预期:** 文本框回到原始 cinnox soul(没有任何 marker);徽章回到 **"default"**;Revert 再次禁用。
- **记录:** [x ] 通过  [ ] 失败  备注:________________

---

## Part D —— 新会话是否真的用上了编辑后的 soul(机制验证)

### D1. 编辑一个标记并保存
- **操作:** 打开编辑器,在最顶部加一行 `MANUALTEST-TAKEEFFECT`,点 **Save**(徽章 "customized")。
- **预期:** 保存成功,徽章 "customized"。
- **记录:** [x ] 通过  [ ] 失败  备注:________________

### D2. 开一个新会话
- **操作:** 在新标签页打开 `http://127.0.0.1:10142/chat/cinnox`。**若首次显示 "Could not reach the assistant" 就刷新一次页面**(等 agent 热起来),输入一句(例如 "你好,你们是做什么的?")并发送,等约 20-40 秒(claude 生成)。
- **预期(本机):** ✅ 收到真实 AI 回复,且**回复体现你编辑的 soul**(若加了 `MANUALTEST-TAKEEFFECT` 之类标记/指令,回复会反映它)。这就是「新会话用上编辑后的 soul」的端到端验证。
- **记录:** [ ] 通过  [ ] 失败  备注:________________

### D3. 收尾(重置)
- **操作:** 回到编辑器,点 **Reset to default** → Confirm。
- **预期:** 徽章 "default"。
- **记录:** [ ] 通过  [ ] 失败  备注:________________

---

## 总体结论

- 通过 ___ / 失败 ___ / 待定 ___
- 是否可以进入下一步:[ ] 可以  [ ] 需要先修问题
- 总体备注:________________________________________________

---

## 附:本次未覆盖 / 已知限制

- **AI 回复的端到端(编辑 soul → 新会话 → AI 按新 soul 回复)** 在本机被 bridge 握手超时挡住(环境问题,工作机正常);本计划用「服务器端验证新会话 agent 的 soul」替代。
- **非管理员被拒**(config-admin 能力门槛)由单元测试 `config_auth_test.exs` 覆盖;手动可选(见 A3)。
