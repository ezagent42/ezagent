# HANDOFF 2026-05-31 — phase-2 customer-service(切回工作机继续)

> 今天在 home/fresh Mac 上收尾;明天切回**原来那台工作机**继续。本文件已提交,跨机器可见。
> (注:`CLAUDE.local.md` 是 home Mac 专属、gitignored,**不会**跟到工作机——工作机有自己的环境约定:共享 deps 缓存 `.poc-shared-deps`、"永不 `mix deps.get`" 等,按工作机原有规矩来,别套用 fresh-machine 的命令。)

## TL;DR — 一句话状态

phase-2 客服 PoC 本机 e2e 全打通(scope #1 可编辑 soul + AI 客服回复 + 多 agent 并发),代码全 push 到 `poc/phase-2-customer-service`(PR **#446**,DRAFT)。**唯一卡点:在等 @allenwoods 拍板一个架构决策(B:让 per-conversation session ephemeral)**,他回复前**不要动核心代码实现 B**。

## ⛔ 阻塞决策(最重要,先看)

**已在 PR #446 comment 里 @allenwoods 提出**(issuecomment-4587036717):
- **问题:** 临时的 per-conversation cc agent + per-session session/orchestrator 会无界累积、boot 时全部重启 → boot 风暴堵死新会话。
- **已 ship 的 plugin-local 止血(Approach A,不动核心):** create_agent 后用 `Workspace.remove_template/2` 注销临时模板 + `mix ezagent.customer_chat.gc_ephemeral` 清扫 task。**已验证:修掉了 `session_templates` 这条路径。**
- **但不充分(已 empirical 确认):** 持久化的 **Session Kind 在 boot 恢复其成员 cc agent + orchestrator**,是第二条等量无界路径,plugin 够不着。
- **要 Allen 拍板的 B:** 让 per-conversation session 本身 ephemeral(`create_session(ephemeral: true)` / 非持久会话路径)。动核心 agent/session-create 契约 + invariant 测试 `agent_create_single_path_test.exs`,属架构决策。
- 依据文档:`docs/notes/2026-05-30-ephemeral-agents-allen-note.md`、`docs/superpowers/specs/2026-05-30-ephemeral-cc-agents-design.md`(§EMPIRICAL 验收结果)。

**明天第一件事:看 Allen 在 PR #446 有没有回复。** 同意 B → 按他的方向实施(那是动核心,走 grill/review)。没回复 → 继续做下面不依赖 B 的事。

## ✅ 这一程做完 + 已 push 的(`poc/phase-2-customer-service`)

cc agent 在 fresh machine 可靠启动 + 多 agent A/B:
- `905a6beb` PTY auto-prompt:自动应答 claude "trust this folder" 首次对话框 + 修 dev-channels match(TUI 动画把 "Loading" 拆成 "L ading")。**平台层(`ezagent_domain_pty`),将来可单独 PR 进 main。**
- `e66c90e9` + `0e28900d` per-conversation cwd(+ mkdir):停掉共享 `.mcp.json` 的 bridge-token 踩踏,并发会话各自绑定。
- `1d3d73c7` 冷启动 bootstrap 重试:首屏不再误报 "Could not reach the assistant"。
- `fb0fdc89` + `640588d8` 临时 agent 不累积止血(Approach A,上面那条)。
- 文档:`a2b23c0c` spec、`87ee8980` plan、`fb7da37c` Allen note、`ca005258` T4 empirical。
- (更早:env-size + soul-file 两个 PTY 平台修复;editable-soul scope #1 T1–T8。)

单测全绿(customer_chat 28 + domain_pty auto-prompt 4)。

## 📋 明天的任务(按优先级)

1. **看 Allen 对 B 的回复**(PR #446)。同意→实施 B(动核心,走 review);否则跳过。
2. **录 demo 视频补进 PR #446**(今天没做成——home Mac 上 Chrome `gif_creator` 报 "not in MCP tab group"、`save_to_disk` 截图落不到本地盘)。工作机上 `gif_creator` 可能正常。流程:① 客户咨询→AI 回复;② operator 监听+接手(`/operator/<tenant>` → session view → take over → 客户端显示"客服已接管");③ admin 改 soul→新会话行为变化。
   - **③ 的可靠做法(已验证,别走弯路):** 做干净的 before/after,就**原地改一条现有事实的值**——已验证:把 soul 里「标准版每月 **USD 39**」改成 **USD 88**,新会话问价格答的就是「标准版每月 USD 88/位用户…」。**不要**用"末尾追加一条和现有护栏冲突的促销/格式前缀"——那会被模型正确无视(我之前误把这个当成"改 soul 无效",是错的)。机制 100% OK(编辑后的 soul 进了新 agent 的 `.esr-system-prompt.md`,grep + USD 88 双证)。规律:**改那条规则本身 = 生效;在旁边加一句对着干 = 不可靠。**
3. **小 UI 收尾**(早先手测发现,非阻塞):B2 = ConfigLive 保存后不显示"已保存"flash(只渲染了 `:flash_error`);A2 = DashboardLive "Customer Sessions" 标题对比度太低。
4. **平台修复进 main 的 PR**(可选,等 phase 收尾后):auto-prompt(`905a6beb`)+ env-size + soul-file 这几个 `ezagent_domain_pty`/平台层修复适合单独 PR 给 main,cherry-pick 即可。

## 运行 + 验证速记(工作机按自己环境调整)

- 服务:`EZAGENT_PROFILE=poc-phase2 ... mix phx.server`(端口 10142;具体 env 见工作机原有 setup)。
- 跑 customer_chat 测试:进 `apps/ezagent_plugin_customer_chat` 直接 `mix test`(本机 `mix cmd --app` wrapper 会报 `File.cd!` 错,工作机不一定)。
- gc 清扫(server 停止时跑):`mix ezagent.customer_chat.gc_ephemeral`(幂等;清 `cc.agent.cc_cust_*` 注册 + `cc_cust_*` 快照)。
- A/B 并发验证:开两个不同 conv,各发消息,各自独立回复(已验证 `{:both_bound,1}` + 两个不同正确回复)。
- 重启后 boot 干净度:`pgrep -f "esr-system" | wc -l` 应只剩正式 agent(`cc_cs_main`)——注意:**完成过 session-join 的会话目前仍会被 boot 带回来**(就是上面 B 要解决的)。

## Resume 提示词(明天在工作机粘给我)

```
继续 phase-2 customer-service 收尾。先读 poc/phase-2/HANDOFF-2026-05-31.md。
分支 poc/phase-2-customer-service(PR #446)。
第一件事:检查 @allenwoods 在 PR #446 有没有回复关于 "B:ephemeral sessions" 的架构决策——
  - 同意 → 按他的方向实施 B(动核心 create_session/session 持久化 + invariant 测试,走 grill/review,别自作主张)。
  - 没回复 → 不碰 B,改做:① 录 demo 视频补进 PR #446(gif_creator;流程见 handoff 任务2,注意 soul 护栏那个坑);② 小 UI 收尾 B2/A2。
这是工作机,按工作机原有环境约定来(共享 deps 缓存、永不 deps.get 等),CLAUDE.local.md 是 home Mac 专属、不在这台机器。
```
