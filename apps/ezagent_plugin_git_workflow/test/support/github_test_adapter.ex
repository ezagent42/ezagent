defmodule GitHubTestAdapter do
  @moduledoc """
  Test-only provider adapter for E2-A concurrency and schema tests.
  Registered in Store's @known_adapters map for atom-safe DB round-trips.
  """
end
