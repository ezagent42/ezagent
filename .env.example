# 本地环境变量模板。复制为 `.env` 并填入真实值(`.env` 不进仓库)。
#   cp .env.example .env
# 由 config/runtime.exs 在 dev/test 启动时加载;真实环境变量优先于此文件。

# loom 插件调用 DeepSeek 大模型用的 API key(从 DeepSeek 控制台获取,形如 sk-...)。
# 缺失/为空 → loom 返回 :no_api_key,前端弹红色错误卡片。
DEEPSEEK_KEY=

# 可选:profile / home 覆盖(默认 ~/.ezagent / default)。
# EZAGENT_PROFILE=loom
# EZAGENT_HOME=~/.ezagent
